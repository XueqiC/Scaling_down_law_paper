\section{Experimental Details}
\label{sec:app_details}

This appendix records the probes, the compression protocols and the controlled panel in the form
used throughout the paper, so that each later section can be read against them.

\begin{figure}[tb]
\centering
\includegraphics[width=\textwidth]{figs/fig4_legend.pdf}\\[1pt]
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/fig4_a.pdf}\caption{Pruning}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/fig4_b.pdf}\caption{Per-channel quantization}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/fig4_c.pdf}\caption{Grouped quantization}\end{subfigure}
\caption{Compression responses on the controlled Pythia panel. (a) Loss change against retained density on the nine development states, with the median predictions as heavy lines. (b) Loss change against bit-width under per-channel round-to-nearest. (c) Loss change against group size at 3, 4 and 5 bits for one state; groups 32 and 512 were unseen when the predictions were pre-specified.}
\label{fig:pythia_responses}
\end{figure}

\subsection{Probes and loss computation}
\label{sec:app_probes}

Each capability is measured on a fixed probe set of prompt and reference pairs, drawn once with a
fixed seed from held-out benchmark splits that are disjoint from all training and recovery sources
used in the paper. The primary benchmarks are MATH-500 \citep{hendrycks2021math,lightman2023verify}
for mathematical reasoning, MBPP \citep{austin2021program} for code generation, and 2WikiMultihopQA
\citep{ho2020constructing} for multi-hop question answering. For each capability we draw 128 probes
and split them by index parity into an estimation half, used only for gradient and Fisher
statistics, and a measurement half of 64 probes on which each reported loss is computed, so that
mechanism statistics and evaluations do not share sampling noise. All reported results use the
64-probe measurement half unless a 512-probe re-measurement is stated.

The secondary benchmarks of the measurement checks (Appendix~\ref{sec:app_measure}) are GSM8K
\citep{cobbe2021gsm8k}, HumanEval \citep{chen2021codex} and HotpotQA \citep{yang2018hotpotqa} on the
pruning panel, where no training is involved, and SVAMP \citep{patel2021svamp}, HumanEval and
TriviaQA \citep{joshi2017triviaqa} where distilled models are scored, chosen so that no benchmark
from which distillation traces were derived is used to score a distilled model; MuSiQue
\citep{trivedi2022musique} serves as a second multi-hop distribution. A sample-level audit found no
overlap between any measurement probe and any training pool. The models are Pythia
\citep{biderman2023pythia}, Gemma 3 \citep{gemma2025gemma3} and Qwen3 \citep{qwen2025qwen3}, adapted
with LoRA \citep{hu2022lora}; the size-and-stage panel follows the observational use of released
checkpoints \citep{ruan2024observational,polo2024tinybenchmarks}.

\subsection{Compression protocols}
\label{sec:app_compress}

The three operations are fixed by protocol. Pruning is global magnitude pruning over every weight matrix of the language model, the attention and MLP projections together with the embedding and output-head matrices, which removes the smallest weights under one threshold to a target retained density $\dens$; biases and norms are untouched, and the prune scope recorded for each model is its full matrix parameter count. The global threshold is estimated from a seeded sample of
absolute weights, which lets the same procedure scale to 30B-parameter models, and each density is
applied to the same dense reference weights, so the densities are nested, and each pruned set contains the previous one.

Per-channel quantization fake-quantizes the weights per output channel with symmetric
round-to-nearest at bit-width $\bits$: integers are clamped to $[-q_{\max},q_{\max}]$ with
$q_{\max}=2^{\bits-1}-1$, the per-channel scale is the maximum absolute weight of the channel divided by
$q_{\max}$, and activations stay in bfloat16. The dense weights are restored between bit-widths.
Because the clipping range is fixed, the step size from 4 to 5 bits shrinks by $7/15$ and the
squared step ratio between adjacent bit-widths is $(q_{\max}(\bits)/q_{\max}(\bits+1))^2$ and
not a constant $1/4$. A squared step ratio becomes a loss ratio only under assumptions about the
error distribution and the local response that we do not test, so the $4^{-\bits}$ shape is one
candidate among others and not a derived reference. Nominal storage ratios are computed from
bit-widths; no memory or latency was measured.

The grouped variant of the same quantizer (Section~\ref{sec:law_quant}) splits each row of a
weight matrix into contiguous groups of $g$ input weights, pads the last group with zeros, which do
not affect the group maximum, and applies the same symmetric round-to-nearest with
$q_{\max}=2^{b-1}-1$ per group; $g$ equal to the row width reproduces the per-channel quantizer
exactly. The configurations are $b\in\{3,4,5\}$ and $g\in\{64,128,256\}$, with nominal storage
computed from $b$ and $g$ under the same convention as the per-channel case, so budgets are
comparable across the two quantizers.

Distillation, the third operation, fine-tunes students on black-box teacher traces, a frontier API teacher
answering GSM8K, HotpotQA and CodeAlpaca prompts with 600 traces per domain, under the full-trace
recipe: LoRA adapters of rank 16 and scaling 32 on the query, key, value, output, gate, up and down
projections, AdamW at learning rate $10^{-4}$ unless stated, an effective batch of 16 sequences, and
a cosine schedule with 3\% warmup. The training mode and the trainable parameter count are recorded
in each run manifest. Token accounting distinguishes processed tokens, which count each input token seen including repetitions, unique pool tokens, and supervised completion tokens. The data pool of $U$
examples per domain is either the first $U$ rows of the trace file, the historical protocol, or a
random sample drawn with a pool-sampling seed independent of the training seed; pools are
identified by the sorted hashes of their samples and pairwise overlaps are reported. The historical
schedule spanned a fixed number of epochs, so its cosine horizon varied with pool size; the
controlled matrix of Section~\ref{sec:law_distill} uses a schedule defined on the same absolute
processed-token budget for each pool, and the two protocols are not mixed in any fit or any table of this paper.

\subsection{The controlled Pythia panel}
\label{sec:app_pythia}

\begin{table}[tb]
\normalfont
\centering\footnotesize\setlength{\tabcolsep}{4pt}\renewcommand{\arraystretch}{1.15}
\caption{Cohorts of models and states, compression interventions, and study roles. Rows distinguish development cohorts, pre-specified prediction tests, and descriptive \mbox{post-hoc} measurements; each cohort is scored with the same 64 probes per capability. Densities and reuse counts are dimensionless, bit widths are in bits, and budgets are in tokens. The 2.8B Pythia checkpoints are excluded because their available revisions loaded identical weights, one state under three names.}
\label{tab:cohorts}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}>{\raggedright\arraybackslash\hspace{0pt}}p{\dimexpr 0.176923077\textwidth-1.061538462\tabcolsep\relax}>{\raggedright\arraybackslash\hspace{0pt}}p{\dimexpr 0.307692308\textwidth-1.846153846\tabcolsep\relax}>{\raggedright\arraybackslash\hspace{0pt}}p{\dimexpr 0.253846154\textwidth-1.523076923\tabcolsep\relax}>{\raggedright\arraybackslash\hspace{0pt}}p{\dimexpr 0.261538462\textwidth-1.569230769\tabcolsep\relax}@{}}
\toprule
Cohort & Models and states & Interventions & Role in the study \\
\midrule
Heterogeneous panel &
Twelve public checkpoints from \nSeries{} series and \nFamilies{} families: Qwen3 (0.6B, 1.7B, 4B),
Gemma-3 (270M to 27B), Gemma-4 (31B), Muse (30B), OLMo-3 (7B, 32B); two further Qwen3 checkpoints
(8B, 14B) measured prospectively at four densities. &
Global magnitude pruning at ten densities in $[0.3,\allowbreak 0.9]$; per-channel round-to-nearest quantization
at 8, 6, 5, 4 and 3 bits. &
Establishes the phenomena and the limits of cross-family transfer; screens the measurement noise. \\
\addlinespace
Controlled Pythia panel &
Development states: 160M, 410M and 1.4B at steps 16k, 64k and 143k (nine states). Held-out states:
160M, 410M and 1.4B at step 96k; 1B at steps 32k, 96k and 112k; 6.9B at steps 32k and 112k. &
Pruning at four development densities (0.9 to 0.6) with tests at 0.75, 0.65 and 0.55; quantization at
8, 6, 4 and 3 bits with a test at 5 bits. &
Development set for pruning and quantization; tests along the source axis and the configuration
axis. \\
\addlinespace
Distillation, controlled &
The nine Pythia development states as students; one fixed teacher pool of 600 traces per domain;
Low-rank adaptation adapters under one recipe. &
A fixed distillation recipe. &
Tests source transfer of the signed response $\delta_c$. \\
\addlinespace
Distillation, pools &
Gemma-3-1B student; pools of 75 and 600 traces per domain for fitting and 225 for testing; three
pool seeds and two training seeds. &
Processed-token budget $T$ and reuse ratio $E=T/D_U$. &
Tests transfer to an unseen trace pool. \\
\addlinespace
Distillation, heterogeneous &
Gemma-3 students from 270M to 12B, OLMo-3-7B and Qwen3 0.6B to 4B; two teachers accessed through an application programming interface; full traces compared with answer-only traces. &
Varied distillation recipes. &
Describes measured responses. \\
\bottomrule
\end{tabular*}
\end{table}

The panel uses the public Pythia checkpoints at sizes 160M, 410M and 1.4B and pretraining steps
16{,}000, 64{,}000 and 143{,}000, which correspond to 33.6B, 134.2B and 299.9B processed tokens at
the published batch of $1024\times2048$ tokens per step. The size coordinate $N_0$ counts transformer-matrix parameters only, the query, key, value, output and MLP matrices of each layer, computed from the architecture configuration, and the training-token coordinate is
$D_0=\text{step}\times2{,}097{,}152$. The sources added for the prospective tests, listed in Table~\ref{tab:cohorts}, entered no fit or model selection before their predictions were pre-specified, and they are reported as pre-specified tests.

Before any of these checkpoints entered a fit, the weight identity of each one was verified.
SHA-256 hashes of the safetensors blobs of all 23 Pythia revisions used, 30 unique blobs, show that
each revision with pruning or quantization measurements resolves to a distinct weight set, with one
exception: the Pythia-2.8B revisions published for steps 16k, 64k and 143k load identical learned
parameters. Those revisions were excluded from the panel, and the 2.8B repeatability check
(Appendix~\ref{sec:app_results}) is one state measured twice. The three checkpoints of the pruning confirmation panel have shard hashes that differ
from all other revisions of the same size, and their dense capability vectors differ from those of the neighbouring stages of the same size.



The parameter conventions follow from the architectures. All models in
the heterogeneous panel and the controlled panel are dense
transformers (Section S1 of the supplement); no sparsely activated model
entered any fit, so total and per-token active parameter counts
coincide throughout, and the reference-model definition of
Section~\ref{sec:loss} is exercised only on dense architectures. The laws use the transformer-matrix count $N_0$ of the decoder blocks, excluding embeddings, head, biases and norms; the pre-specified
distillation source term used the metadata parameter count of the student recorded when the predictions were fixed,
which for Gemma-3-4B includes 0.42B vision and projector parameters
and counts tied embeddings twice; these pre-specified covariates are reported as recorded when the predictions were fixed.



\begin{figure}[tb]
\centering
\includegraphics[width=\textwidth]{figs/lr_pilot_legend.pdf}\\[1pt]
\begin{subfigure}[t]{2.7in}\centering\includegraphics[width=\linewidth]{figs/lr_pilot_a.pdf}\caption{Final loss change against peak learning rate}\end{subfigure}
\caption{Learning-rate pilot for the 1B (dashed) and 4B (solid) students at one pool: final-checkpoint
loss change per capability against the configured peak learning rate; positive values indicate a
loss above that of the initial student at the same pool and budget.}
\label{fig:lr_pilot}
\end{figure}

\section{Measurement Validity}
\label{sec:app_measure}

The capability-specificity, same-capability agreement, independent multi-hop distribution, and
pre-specified eight-state scope checks are reported in Section S3 of the supplement.

The remaining two checks concern the link between loss and behavior and the choice of reference
condition. Loss-to-accuracy links were fit on
GSM8K accuracy, MBPP pass@1, and TriviaQA exact match and token F1
across seven pruned models; all four outcomes are compatible with one
cross-family sigmoid in the loss, with no resolved family-specific
term. A lower QA loss does not by itself imply better QA behavior: the
four pruned OLMo-3-32B cells whose QA loss lies below dense show no
increase in exact match. This audit covers pruning only and is not repeated for the other two methods, whose public panels
are smaller.

The reference condition was also varied. For a subset of models the
direct-answer and given-reasoning-answer conditional distributions were
measured in addition to the full-solution reference; they differ from
it for math and QA and coincide for code, where the reference is the
function body, so the numbers in this paper are specific to the
full-solution reference and should not be read as evidence of robustness to the reference format.

The loss is the conditional cross-entropy of the reference completion
given the prompt, summed over completion tokens and divided by the
number of completion tokens, as in Eq.~\ref{eq:taskloss}. Prompts and
references are tokenized with the tokenizer of each model and truncated to
1024 tokens; prompt tokens are masked out of the loss. All within-model
responses $\Delta L_c$ are differences in native-token nats
on identical token sequences. Because native-token units differ across
tokenizers, cross-family comparisons of absolute levels use per-byte
normalization or are reported separately, and no cross-tokenizer
quantity is treated as a common numerical scale. Negative responses are
retained as measured and are not clipped at zero.

\begin{figure}[!t]
\centering
\includegraphics[width=5.5in]{figs/measurement_support_legend.pdf}\\[1pt]
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/measurement_support_a.pdf}\subcaption{Math transfer MAE}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/measurement_support_b.pdf}\subcaption{Code transfer MAE}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/measurement_support_c.pdf}\subcaption{QA transfer MAE}\end{subfigure}\\[3pt]
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/measurement_support_d.pdf}\subcaption{Math paired gain}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/measurement_support_e.pdf}\subcaption{Code paired gain}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/measurement_support_f.pdf}\subcaption{QA paired gain}\end{subfigure}

\caption{Measurement support for capability specificity. (a)--(c) Out-of-fold MAE of predicting the secondary-benchmark response of a capability from the primary-benchmark response of the same capability (diamond) or of another capability, against zero and train-mean controls, on the six-model, five-density pruning panel; protocols 1--3 hold out models, densities, or both, and 4--6 repeat them at $d\ge0.75$. (d)--(f) Paired same-over-cross gain with 95\% model-bootstrap intervals over models.}
\label{fig:measurement}
\end{figure}



\section{Heterogeneous-Model Results}
\label{sec:app_hetero}

This section reports the twelve-model panel that Fig.~\ref{fig:heterogeneity} summarises, first
under pruning, then under quantization and distillation, and closes with what the loss endpoint
does and does not establish about the behavior of the compressed models.

\input{tables/panel_prune}
\input{tables/panel_quant}
Under pruning, Table~\ref{tab:panel_prune} gives per-capability
damage at two densities, the density at which math damage first exceeds
one nat, and how QA ranks before that cliff. Magnitudes differ by two orders across series at the same density: OLMo-3-32B loses 0.01 nats on math at $d=0.7$ where Gemma-3-27B loses 4.0. The ordering of capabilities is
series-specific: in the Qwen3 sizes and in OLMo-3-32B, QA is the
least-damaged capability at each pre-cliff density and improves, by up
to $-0.79$ nats at Qwen3-4B; in each Gemma-3 size, in Gemma-4-31B,
and in Muse-30B, QA is the most-damaged capability at the mildest densities, by a factor of four on Gemma-4-31B;
OLMo-3-7B lies in between with differences of at most $0.15$ nats. A
measurement-noise floor sets the scale: the median $|\Delta L|$ at $d=0.9$ is
$0.013$ nats, and 29 of the 62 nominal improvement cells across all
arms exceed twice that median; the rest are treated as zero. Cliffs occur between $d=0.4$ and $0.7$ for Qwen3 and Gemma, at $0.55$ for Muse-30B, $0.35$ for
OLMo-3-7B and $0.2$ for OLMo-3-32B. Robustness is not monotone in size within a series: Gemma-3-12B is the most robust Gemma-3 size and 27B collapses earlier.
A predictor has to reproduce this heterogeneity, which no single property of a model family, size or training length included, determines by itself.

\paragraph{A second pruning criterion.} Wanda \citep{sun2023simple} scores each weight by its
magnitude times the norm of its input activation and prunes each output row of the block linear
layers, leaving embeddings dense, so its density counts a different scope from the panel's global
magnitude criterion. Table~\ref{tab:wanda-panel} measures it on Qwen3 4B and Gemma 3 4B at seven
densities. The criterion moves the amplitude of the response by an order of magnitude: at density
0.3 Gemma 3 4B loses 20.6 nats on mathematics under magnitude pruning and 2.1 under Wanda, and
Qwen3 4B 12.3 against 1.7. The shape in density travels better than the amplitude: with the exponent
of the power form pooled from the magnitude cells and only the scale refit, the Wanda responses are
matched to 0.15 and 0.08 nats on mathematics and 0.23 and 0.29 on code over the seven densities,
against 0.18, 0.14, 0.26 and 0.17 with a free exponent. A relation fitted under one criterion thus
transfers its shape and not its scale, and the scale has to be measured under the new criterion; a
single calibration at the mildest density does not supply it, for the reason given in
Section~\ref{sec:efficiency}, that the response there lies within measurement noise. Question
answering keeps its own sign: under Wanda it improves on both models at every density from 0.5
down, where magnitude pruning improves it only on Qwen3 and only above density 0.5.
\input{tables/wanda_panel}

Under quantization, Table~\ref{tab:panel_quant} covers
14 models. Int8 and int6 stay within $0.01$ nats of dense. Int4 increases the loss of small models by one to two nats and changes large models by a few tenths; a QA
improvement under int4 recurs in five models and exceeds the measurement-noise floor. Int3 collapses 12 of the 14 models by $4.6$ to $28$ nats;
OLMo-3-7B loses $2.8$ to $4.7$ and OLMo-3-32B only $0.4$ to $1.2$, so
the collapse is common although not universal, and OLMo-3 is the robust
series under both interventions. Within the tested bit-widths a learned
exponential $q_c e^{-kb}$, with amplitude $q_c$ per capability and rate $k$, fits held-out bits better than the fixed $q_c4^{-b}$ shape, 6.7 against 7.6 nats pooled, which shows only
that the fixed base does not describe this quantizer; the squared
step-size ratio between 4 and 5 bits is $(7/15)^2\approx0.22$
(Appendix~\ref{sec:app_details}), and a loss ratio would follow only under assumptions about the error distribution and the local response that lie outside the scope of this
study.

Under distillation, black-box traces from two API teachers, 600 per domain, train Gemma-3 students under a full-trace and an
answer-only recipe in a paired design. The answer-only recipe increases the math loss about twice as much as the full recipe, while QA improves by 0.6 to 1.4 nats under both; because the recipes differ in supervised tokens and update
counts, this pairing does not isolate reasoning coverage, and the
controlled comparisons that do isolate it are the ones reported in Section~\ref{sec:law_distill} under the controlled recipe.

The loss endpoint supports these comparisons within limits. The same
heterogeneity appears on independent secondary benchmarks for math and
QA, where the primary-benchmark response predicts the secondary one
better than the response of any other capability does, with intervals
excluding zero in most hold-out protocols; for code the
cross-capability predictor is as good or better
(Appendix~\ref{sec:app_measure}, Fig.~\ref{fig:measurement}). A lower
QA loss does not by itself mean better QA behavior: in the four pruned
OLMo-3-32B cells whose QA loss lies below dense, exact-match accuracy
does not rise. ``QA improves'' in this paper refers to the loss and to nothing else.


\subsection{Scale and family structure}
\label{sec:family}

Within a family, robustness is not monotonic in size, and this holds in two families
independently: Gemma-3 robustness peaks at 12B and 27B collapses far earlier, in line with the earlier
ability-space observation of a Qwen3 14B peak. A mid-scale robustness peak appears in both families, in the present grid and in the earlier one.
One candidate predictor of this structure was retired. The
top-concentration statistic $\kappa_c$ of the capability gradient tracked robustness within the
Qwen3 ladder and was pre-specified as a predictor. The full grid does not support it: across families the
ordering inverts, since OLMo, the most robust family, has the lowest concentration, and within
Gemma-3 the largest size violates the within-family trend, so the statistic was retired.
At matched scale, on the panel of models near 30B parameters, the
family coefficient dominates: $\Delta\Lc{\text{math}}$ at $\dens=0.7$
spans two orders of magnitude, from 0.02 nats on OLMo-3-32B through 0.17 on Muse-30B and 0.68 on Gemma-4-31B to 4.04 on Gemma-3-27B. OLMo-3-32B survives $70\%$ pruning with
$\le+0.63$ nats on each capability. Family identity is a first-order determinant of prunability on this panel; which training-data or recipe properties carry it was not measured and is left open on this panel.



\section{Fitted Models and Coefficients}
\label{sec:app_coef}

The delivered pruning form (Eq.~\ref{eq:power}) and its standardization are in
Table~\ref{tab:round3_coef}. The coefficients of the grouped-quantization surface, which is not a
delivered relation, are in Section S2 of the supplement beside the analysis of its rank. The per-bit
source regressions, the distillation source and pool forms, and the pre-specified multi-student forms are
stored with their standardization constants in the development files listed in
Appendix~\ref{sec:app_registration}.
\input{tables/round3_coef}

\section{Relation Selection, Boundaries and Calibration}
\label{sec:app_forms}

This section records how each delivered relation was selected, where its boundary lies, which
capabilities it serves, and what structure the relations share.

\subsection{Selection and boundary of the delivered relations}
\label{sec:app_selbound}

The pruning relation was selected on the development panel. Fit on nine states at $d\in\{0.9,0.8,0.7,0.6\}$
and scored at the unseen 0.65 and 0.55, Eq.~\ref{eq:power} reduces the
strength-only error by a factor of two to three, and A2 matches it; refit on 17
states under a pre-specified leave-one-source-out rule, A2 and the power form tie within 0.02 nats, at 0.479 and 0.494, and the rule prefers the continuous form that vanishes at $d=1$; the coefficients are in Appendix~\ref{sec:app_coef}, and the exponent $\gamma_c$ is a fitted constant whose value is supported among the same-complexity alternatives tested in Table~\ref{tab:a14-baselines}: forms with the exponent fixed at one or replaced by a quadratic are worse by 0.04 to 0.20 nats on mathematics and code at both budgets.

Its boundary was then established. Evaluated as a pre-specified candidate on three new checkpoints, the power form reaches the math error of A2 with five parameters against twenty and does not beat the median curve on code or QA; on a 2.8B source the median curve is three to seven times better than either source form (Section~\ref{sec:general}). The compact form trades accuracy for compactness within a tested range; the median curve is the relation for new states, a rule fixed after these tests and reused without change in Section~\ref{sec:selection} for the selection rule and its confirmation.

For quantization, two parametric candidates were tested first, a
separable amplitude-times-shape form and a low-order surface in
$\log_2 q_{\max}$ and $\log_2 g$; the surface beat zero change at the
unseen $b=4$ and $g=128$ on the two-bit-level development set, but its quadratic term is unidentified there, with design rank 16 of 20, as Section S2 of the supplement shows. That earlier round contains the only test of a genuinely unseen bit width, and it is
reported here rather than in Table~\ref{tab:main-prediction-v2}. On the six development states at
bit width 4 with group sizes 64 and 256, fitted on 24 development configuration measurements per
capability, the pre-specified surface reaches 0.19, 0.21 and 0.44 nats on mathematics, code and question
answering, against 0.55, 0.73 and 0.54 for the development-selected median. The surface is not the
delivered relation, and the relation that is delivered can show no error of its own on these cells,
because they were added to the development grid of the round that selected it; the interpolation
variant scored on them in the earlier round uses different models and boundary rules and does not
stand in for it. Table~\ref{tab:main-prediction-v2} therefore carries only tasks on which the
delivered predictor has an independent error. In a confirmation round with all 54
unblinded cells as development set and 21 cells pre-specified at unseen
group sizes and on a new state, the interpolation won the pre-specified cells
for math and code and the median won the new state, so the delivered
rule was fixed after that round and is marked Retrospective in
Table~\ref{tab:main-prediction-v2} and in the tables of
Appendix~\ref{sec:app_results}. Table~\ref{tab:main-prediction-candidates} lists the pre-specified
candidate of each task beside the relation delivered after its test.
\input{tables/main_prediction_candidates}

The boundary of the quantization relation follows from the same tests: the interpolation holds at unseen group sizes of
seen states; on the new state the source-free median is as good or
better except for code, so source conditioning adds nothing there,
and the surface is not delivered, as the tests of Section~\ref{sec:unseen_settings} confirm on each
held-out cell of that panel.

For distillation, eight forms in $T$, $E$, and a source term were
fixed on twelve development runs and scored on nine unseen-pool
trajectories with the headline chosen after the test, and two
descriptor-carrying forms were compared retrospectively
(Section S2 of the supplement); the delivered forms were then
re-selected on all 25 trajectories by a pre-specified
leave-one-trajectory-out rule and pre-specified for six new pools of
$U{=}200$ at three new budgets. A linear form in the student state, fit on nine Pythia states, serves only as the math predictor for the distillation candidates of the selection rule in
Section~\ref{sec:selection}, and enters no other relation.

The boundary of the distillation relation is set by the new-pool test. At the new pool size the
selected form beats the
pre-specified strongest baseline for code on both students, where that
baseline is the same $\log(1+E)$ form with an intercept, so the gain
supports the zero-budget boundary and the parsimony of the form and does not
establish a full student-data-training law; QA gains on 270M only, math loses to intercept baselines, no selected form is the post-hoc best of the sixteen pre-specified forms, a minimum selection favours by construction (Section S4 of the supplement); a student 6.6 times the reference size lies outside the tested range.


\subsection{Applicability across capabilities}
\label{sec:app_applic}

Where the initial-state inputs help is method- and range-specific:
they carry math and code at unseen pruning densities of seen sizes
and at unseen group sizes of seen states, add nothing on new
quantization states or in distillation beyond the reference anchor,
and over-predict the QA pruning response on each new checkpoint. What capability conditioning adds is measurable on the pre-specified
confirmation panels (Appendix~\ref{sec:app_shared}): per-capability
pruning curves beat a shared response curve with a signed
per-capability scale by 0.06 nats macro-averaged, largely through QA at 0.17 nats, whose aggregate curve is not proportional to the tested shared curve:
it changes sign across density, with a loss increase at $d=0.55$ and an improvement from 0.6 to 0.9, so no scale of that curve, including a negative one, represents it, and its direction also varies across sources. For math and
code a shared curve is as good or better, for grouped quantization the two are indistinguishable, and for distillation the
comparison is empty because the one-coefficient forms are
algebraically equivalent. Conditioning matters where the shape of the
response differs across capabilities.
\input{tables/cap_conditioning}

Shared structure and parameter ranges complete the picture. One function family across
methods holds for pruning by construction and fails for quantization
and distillation; within pruning one exponent shared across
capabilities costs nothing, whereas quantization coefficients must be
capability-specific; parameter intervals and 80\% prediction intervals that under-cover are reported in Appendix~\ref{sec:app_shared} together with the one-point calibration and the single measurement that it costs.


\begin{figure}[tb]
\centering
\includegraphics[width=5.5in]{figs/final_relations_legend.pdf}\\[1pt]
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/final_relations_a.pdf}\subcaption{Pruning}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/final_relations_b.pdf}\subcaption{Math, 3 bit}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/final_relations_c.pdf}\subcaption{Math, 4 bit}\end{subfigure}\\[3pt]
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/final_relations_d.pdf}\subcaption{Math, 5 bit}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/final_relations_e.pdf}\subcaption{270M}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/final_relations_f.pdf}\subcaption{1B}\end{subfigure}

\caption{Measured responses and pre-specified relations on native configuration axes. (a) Pythia-1.4B at step 112k: measured densities, the delivered power form (solid, Eq.~\ref{eq:power}) and the pre-specified median development curves (dashed); shading marks the refit range $d\in[0.55,0.9]$. (b)--(d) Math responses to group size at 3, 4 and 5 bits on Pythia-410M at 143k (solid) and 1.4B at 16k (dotted): pre-specified piecewise interpolation, per-configuration medians (dashed), and predictions at the unmeasured $g=32,512$ (hollow) with their later measurements (filled). (e)--(f) Gemma-3 270M and 1B trajectories at $U=200$: measured responses at the recorded reuse ratio, the pre-specified selected forms (capability colours) and the strongest pre-specified baseline (grey), all on the native configuration axes.}
\label{fig:responses_relations}
\end{figure}

\subsection{Shared structure, parameter ranges, and one-point calibration}
\label{sec:app_shared}

Fig.~\ref{fig:responses_relations} plots the measured responses against the delivered relations on
each method's own configuration axis. One function
family for all three methods, with method- and capability-specific
parameters, holds for pruning by construction and fails for the other two arms on
the same held-out splits. Within pruning one exponent shared across capabilities costs nothing, within 0.02 nats of per-capability exponents, whereas the quantization coefficients must be capability-specific. Three ranges are kept apart: the bootstrap intervals of the pruning coefficients exclude zero for
the dense-anchor term on math and code and not on QA; per-source exponents spread widely, from 3.0 to 6.0 for math; and 80\% prediction intervals from leave-one-source residuals cover only 67 to 78 percent of held-out cells at a mean full width near 1.5 nats. One-point calibration of the pruning amplitude at the mildest density
amplifies noise and offers no saving over the uncalibrated predictor.




Table~\ref{tab:shared_structure} reports the shared-family test, the parameter-sharing tests and
the spread of exponents fitted separately per source. What one target measurement would buy, a
budget no delivered relation uses, is in Section S2 of the supplement.
\input{tables/shared_structure}


The value of capability conditioning can be measured on the same confirmation panels. At equal
development information, Table~\ref{tab:cap_conditioning}
compares the delivered per-capability source-free predictors, variant A, with three shared alternatives: one shared response curve times a signed per-capability scale, variant B; the shared curve plus a per-capability offset, variant C; and the shared curve alone, variant D, all fit on development data only and scored on the
held-out confirmation cells. With the two identical 2.8B labels merged into one state, the gain of A over B is 0.059 nats macro-averaged for pruning, 0.168 for QA, and within noise for math and code; 0.003 for quantization; and zero for distillation, where the single-coefficient forms of A and B coincide, which
supports no conclusion about conditioning there. The audit of this
comparison (Section S2 of the supplement) shows that negative scales were allowed
and none occurred, and that the QA gain arises because the aggregate
QA curve reverses sign across density, a shape no proportional
baseline can represent; the direction of QA also varies across
development sources, which neither source-free variant captures. The ablation is retrospective and is reported as such.



\section{Pre-specified Prediction Results}
\label{sec:app_results}

This section collects the pre-specified predictions and their measured outcomes, cell by cell, in the
order in which the tests were run and the predictions were pre-specified.

\begin{figure}[tb]
\centering
\includegraphics[width=\textwidth]{figs/corner_legend.pdf}\\[1pt]
\begin{subfigure}[t]{2.7in}\centering\includegraphics[width=\linewidth]{figs/corner_test_a.pdf}\caption{QA readout}\end{subfigure}\hfill
\begin{subfigure}[t]{2.7in}\centering\includegraphics[width=\linewidth]{figs/corner_test_b.pdf}\caption{Math and code readouts}\end{subfigure}
\caption{The four-corner second difference per student and readout on its own scale: the measured
value with its pre-specified interval, the additive prediction of zero and the pre-specified interaction prediction. Both predictions lie inside the QA intervals, so the design did not separate the two
structures; the math and code readouts were specified in advance as underpowered before the trajectories
were trained.}
\label{fig:corner_test}
\end{figure}
\begin{figure}[tb]
\centering
\begin{subfigure}[t]{2.7in}\centering\includegraphics[width=\linewidth]{figs/corner_contrasts_a.pdf}\caption{Prediction minus measurement}\end{subfigure}
\caption{Corner residuals for the two students and the pre-specified additive corner contrasts on the
fresh 2Wiki, MuSiQue and TriviaQA distributions, whose pre-specified additive prediction is zero; the
legend of Fig.~\ref{fig:corner_test} applies to every panel, with the same colours and markers.}
\label{fig:corner_contrasts}
\end{figure}


Section S4 of the supplement gives each pre-specified test in three lines, the candidate, the strongest same-input baseline and the strongest simple baseline, with improvements and provenance codes,
the per-stage and per-protocol results of the Pythia-1B and 6.9B pairs, the confirmation panel and the
grouped-quantization tests, and the paired improvements from adding each input and from each
functional form. Fig.~\ref{fig:corner_contrasts} gives the corner residuals on the fresh
distributions.
The same comparison in one line per test, each relation against its strongest same-input baseline
on the same cells, is in that section as well, with the same per-cell definition of error.
\begin{retargetreferences}{\retargetreference{Appendix Tables~\ref{tab:pred_source}--\ref{tab:pred_config_qd}}{Section S4 of the supplement}}

\begin{figure}[tb]
\centering
\includegraphics[width=\textwidth]{figs/fig3_legend.pdf}\\[1pt]
\begin{subfigure}[t]{\textwidth}\centering\includegraphics[width=\linewidth]{figs/fig3_a.pdf}\caption{New configurations of model states that entered the fit}\end{subfigure}\\[2pt]
\begin{subfigure}[t]{\textwidth}\centering\includegraphics[width=\linewidth]{figs/fig3_b.pdf}\caption{Model states that entered no fit}\end{subfigure}
\caption{Generalization of the pre-specified relations by kind of novelty: each row compares the error of
the relation, a hollow diamond, with the development-selected baseline, a hollow circle, on the
same cells, one sub-row per capability; the segment is coloured where the relation is lower.}
\label{fig:generalization}
\end{figure}

\end{retargetreferences}

The second confirmation round added seven pruning densities from 0.55 to 0.9, of which 0.55 is an extrapolation, and five bit-widths from 3 to 8 on Pythia-1B at steps 32k and 112k, with protocol A fit
on the full nine-state development panel and protocol B on states at step 64k
or below. The 6.9B pair was a size extrapolation of about five times:
under protocol A at 112k, the source regression reaches 0.23, 0.19 and 0.96 nats on math, code and QA against 0.05, 0.07 and 0.15 for the median curve;
at $d=0.55$ the source-free curves win under both protocols, with
source-conditioned errors between 0.55 and 2.2 nats. For quantization under
protocol A the class-indicator candidate is worst or near worst above 3 bits, an order of magnitude behind the per-bit median, and overshoots
by about four nats at 3 bits; protocol B compresses the differences above
3 bits to the 0.02 to 0.05 range and leaves the candidate ahead at 3 bits only for the late stage. The full two-protocol results are tabulated per stage and protocol in Section S4 of the supplement.

The distillation confirmation round re-selected forms on all 25 trajectories of the uniform protocol under leave-one-trajectory-out folds, with ties within 0.02 nats resolved toward fewer parameters, which gave a reuse form for math and code and the joint form for QA, fixed predictions for six new pools
of $U{=}200$ at budgets of 50k, 100k, and 200k supervised tokens, and
fixed the strongest baseline per student and capability from the same development pass, a development-designated baseline; ranked post hoc over all sixteen pre-specified forms, a source-conditioned variant has the lowest test error for each student and capability, as Section S4 of the supplement records. Table~\ref{tab:distill_confirm} gives the paired differences with pool-cluster intervals over six independent pools. Dense losses
re-measured on a different GPU drift by at most 0.001 nats from the
pre-specified descriptor and were tolerated at 0.01; shifting each response
by the drift of its trajectory in either direction changes no interval sign in any of the 12 comparisons. Per-example losses are
not stored, so an item-level interval cannot be computed; each
evaluation scores 64 items per capability, and the pool-cluster intervals therefore cover the sampling of pools and nothing else.
\input{tables/distill_confirm}



\subsection{Measurement efficiency of the compact pruning form}
\label{sec:app_a11}

Fig.~\ref{fig:efficiency_qa} gives the question-answering counterpart of the main-text figure. A retrospective subsampling study of the nine-state development panel, with twenty fixed replicates at 25, 50 and 100 percent of the measurements that reduce either the number of sources or the number of densities per source, found that across the replicates the five-parameter power form fitted on 18
measurements attains median errors of 0.20 nats on mathematics and 0.17 on code, against 0.18 and
0.13 for the per-density regression fitted on all 36, and that the
median curve is the better predictor for QA at every budget. Table~\ref{tab:a9} gives its error at each budget. A pre-specified confirmation then fixed
five predictors before four Pythia states in no earlier fit or test, listed in Table~\ref{tab:a11}, were pruned to densities 0.65 to 0.9;
Table~\ref{tab:a11} gives the outcome. The pre-specified rule, that the reduced-budget power form lies within 0.05 nats of the full-budget regression on math and code, is met.

\begin{figure}[tb]
\centering
\includegraphics[width=\textwidth]{figs/eff_qa_legend.pdf}\\[1pt]
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/eff_qa_a.pdf}\caption{Error against budget}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/eff_qa_b.pdf}\caption{Four new Pythia states}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/eff_qa_c.pdf}\caption{OLMo-2, half against full grid}\end{subfigure}
\caption{Measurement efficiency for question answering, the counterpart of
Fig.~\ref{fig:efficiency}. The median density curve is the better predictor at every development
budget, and on the four confirmation states the three predictors are within 0.006 nats of each
other, 0.177 for the per-density regression, 0.179 for the power form and 0.183 for the median
curve, so nothing here displaces the median curve as the delivered rule for this capability; on
OLMo-2 the reduced-grid form and the full-grid regression are within 0.002 nats on the probes and
0.009 apart on the new items, both far below the zero-change baseline of 0.13 and 0.16.}
\label{fig:efficiency_qa}
\end{figure}

\begin{table}[tb]
\centering\footnotesize
\caption{Pre-specified measurement-efficiency confirmation on four Pythia states outside every fit, 160M at step 80000, 410M at step 112000, 1.4B at step 48000 and 1B at step 48000, with 24 cells per capability: mean absolute error in nats per token of each pre-specified predictor. The
reduced-budget power form used the densities 0.9 and 0.7 of each development state, 18 measurements per capability.}
\label{tab:a11}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}lrrr@{}}
\toprule
Predictor (development measurements per capability) & Math & Code & QA \\
\midrule
Power form (18) & 0.073 & 0.104 & 0.179 \\
Power form (36) & 0.062 & 0.109 & 0.223 \\
Per-density regression (36) & 0.060 & 0.084 & 0.177 \\
Median density curve (36) & 0.112 & 0.170 & 0.183 \\
Median density curve (18) & 0.145 & 0.212 & 0.186 \\
\bottomrule
\end{tabular*}
\end{table}

\begin{table}[tb]
\centering\footnotesize
\caption{Subsampling study on the nine-state development panel: median mean absolute error in nats
on the new-state cells over twenty fixed replicates, when the nine-state development panel retains
9, 18 or all 36 density measurements per capability in total. Where fewer than twenty replicates could be fitted, the count
is given; a slash means no replicate could be fitted.}
\label{tab:a9}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}rlrrr@{}}
\toprule
Measurements per capability & Predictor & Math & Code & QA \\
\midrule
9 & Power form & 0.25 & 0.30 & 0.61 \\
9 & Per-density regression & / & / & / \\
9 & Median curve & 0.33 & 0.47 & 0.58 \\
18 & Power form & 0.20 & 0.17 & 0.52 \\
18 & Per-density regression & 0.24 (6/20 fits) & 0.28 (6/20 fits) & 0.76 (6/20 fits) \\
18 & Median curve & 0.21 & 0.21 & 0.33 \\
36 & Power form & 0.19 & 0.17 & 0.54 \\
36 & Per-density regression & 0.18 & 0.13 & 0.56 \\
36 & Median curve & 0.19 & 0.18 & 0.31 \\
\bottomrule
\end{tabular*}
\end{table}

\paragraph{Second family.} The measurement-efficiency result of Section~\ref{sec:efficiency}
rests on Pythia, so a pre-registered replication repeats the reduced-grid comparison on OLMo-2, whose
stage-1 checkpoints carry their pretraining token counts in their names and differ in weights by
construction; the shards of every checkpoint were hashed before measurement. Six development states,
OLMo-2 1B after 399, 1993 and 3608 billion tokens and 7B after 391, 1947 and 3507 billion, were pruned
at densities 0.9, 0.8, 0.7 and 0.6, the full grid of 24 configurations; the reduced grid keeps the same
six states at 0.9 and 0.7, 12 configurations. Four test states, 1B after 1196 and 2811 billion tokens
and 7B after 1167 and 2727 billion, enter no fit and were pruned at three densities fixed in advance,
0.85, 0.75 and 0.65, after every predictor had been frozen. The five predictors that share the compact
form's inputs were refitted on each grid with the same labels, the same exponent and curvature grids
and one estimator for every linear-in-feature form, ridge on the standardized inputs with the weight
chosen by leave-one-state-out; this estimator was fixed before any 7B or test state entered a fit,
after an unregularised dry fit on the three 1B states alone had produced code predictions of tens of
nats. Losses were measured on the development probes and on 64 new items per capability drawn from no
earlier probe, sample, teacher trace or few-shot exemplar. Table~\ref{tab:second-family} gives the
outcome over the four states and twelve cells per capability. Fitted on the reduced grid, the compact
form errs by 0.017 nats on mathematics and 0.020 on code against 0.019 and 0.027 for the per-density
regression fitted on the full grid, and by 0.062 against 0.060 on question answering; on the new items
the pairs are 0.012 against 0.013, 0.029 against 0.036 and 0.045 against 0.036. A paired item
bootstrap with the four states fixed puts the difference at $-0.002$ [$-0.005$, $+0.001$] on
mathematics, $-0.007$ [$-0.010$, $-0.004$] on code and $+0.006$ [$+0.000$, $+0.014$] on question
answering. Per state, the compact form's code error is below the regression's on three of the four
states and 0.001 above on the fourth, and its mathematics error is below on two and at most 0.004
above on the other two. Among the five reduced-grid predictors the compact form is the most accurate
on mathematics and code and within 0.001 nats of the best on question answering. Question answering is
the noisiest endpoint on this family: in two of the twelve test cells the measured response is smaller
at the harsher density than at the milder one, and every predictor's error there is two to three
times its mathematics error. The reduced grid took 59 percent of the full grid's GPU seconds and 60
percent of its evaluation tokens with dense anchors and model loading included, so half the
configuration measurements is not half the compute. A zero-change baseline errs by 0.080, 0.091 and 0.132 nats on the probes and by 0.087, 0.108 and
0.161 on the new items, so the compact form's error is four to five times smaller on mathematics and
code and half as large on question answering: the panel's responses are not trivial. The Pythia
coefficients applied to OLMo-2 without refitting err by 0.72, 1.88 and 0.38 nats. The pre-specified question, whether the reduced-grid
compact form stays within the 0.020 nats of the full-grid regression that the Pythia confirmation
reached on mathematics and code, is answered in the affirmative, and on this family the form is
below the regression on both; the response structure and its measurement saving replicate across
families, and the coefficients do not transfer without refitting.
\input{tables/second_family}

\paragraph{A second pruning criterion.} Both confirmations above prune by global magnitude, so a
pre-registered replication repeats the Pythia design with the pruning criterion changed to Wanda
\citep{sun2023simple}: every weight of the block linears is scored by its magnitude times the norm of
its input activation over 128 calibration documents of 512 tokens from the C4 validation split, and
the scoring is applied per output row at the target density. The nine development states and the four
test states, the four development densities and the six test densities, the grids, the six predictors,
the estimator rule and the freeze are those of the two earlier confirmations; the activation norms
are computed once per state and reused across its densities, and their time is charged to every grid.
Table~\ref{tab:wanda-efficiency} gives the outcome. Fitted on the reduced grid of 18 configurations,
the compact form errs by 0.023 nats on mathematics and 0.026 on code against 0.025 and 0.027 for the
per-density regression on all 36, and by 0.058 against 0.061 on question answering; a paired item
bootstrap with the states fixed puts the differences at $-0.001$ [$-0.002$, $-0.001$], $-0.001$
[$-0.002$, $-0.000$] and $-0.003$ [$-0.009$, $+0.002$], and on the new items at $-0.002$, $-0.001$ and
$-0.002$. The pre-specified reading, the reduced-grid compact form within 0.020 nats of the full-grid
regression on mathematics and code, is met. Two features of this panel bound what the result shows.
Wanda's responses at these densities are a fraction of magnitude pruning's: at density 0.65 the
mathematics and code losses rise by 0.12 and 0.23 nats on the 160M state and 0.27 and 0.40 on the 410M
state, by 0.02 to 0.06 on the 1B and 1.4B states, and question answering falls by 0.03 to 0.15 nats
on three of the four states. A zero-change baseline therefore errs by only 0.043, 0.058 and 0.036 nats,
so the compact form halves the baseline's error on mathematics and code and has no question-answering
response to predict; on the two largest test states, where every response stays within 0.06 nats,
no predictor improves on zero change, and the comparison that carries information is the 160M and
410M states, where the reduced-grid compact form errs by 0.024 and 0.042 on mathematics and 0.036 and
0.040 on code against 0.029 and 0.037 and 0.046 and 0.031 for the full-grid regression and 0.044 and
0.098 and 0.079 and 0.125 for zero change. The magnitude-pruning coefficients applied to the Wanda
measurements without refitting err by 0.19, 0.20 and 0.27 nats: they predict responses of
magnitude-pruning size. The reduced grid took 70 percent of the full grid's GPU seconds, more than on
the larger models because loading, the dense anchor and the calibration pass are a larger share of a
small model's cost, and 60 percent of its evaluation tokens. The structure and its measurement saving
therefore hold under a second pruning criterion where the criterion produces a response; the
coefficients are specific to the criterion.
\input{tables/wanda_efficiency}

\paragraph{Same-complexity baselines.} Table~\ref{tab:a14-baselines} refits, on the same development
rows and with the same estimator as the compact power form, every form that shares its inputs and a
similar parameter count: the strength relation with the exponent fixed at one, a quadratic strength
relation, the strength-only curve, the median curve and the per-density regression, and the
pruning-law relation $L=L_0P_0d^{\alpha}$ of \citet{sengupta2025compression} fitted on capability
loss with one exponent and one scale per capability. At 18 measurements the compact form is the most accurate on mathematics and code, 0.036 and 0.033
nats ahead of the per-density regression on the same rows; at 36 that regression leads by 0.002 and
0.025, the comparison of Section~\ref{sec:efficiency}, so the compact form's advantage is the
measurements it saves. The fixed-exponent and quadratic forms trail it by 0.04 to 0.20 nats and the
pruning-law form by 0.04 to 0.06 at either budget. Calibrating one number
on each target's mildest pruned measurement, the power form's amplitude or the pruning law's $P_0$,
helps neither form on the remaining densities. These forms were fitted after the confirmation.
\input{tables/a14_baselines}

\paragraph{Prediction of task accuracy.} The pruning law's own endpoint is task accuracy, and
Table~\ref{tab:accuracy-comparison} meets it there: on Gemma 3 1B and 4B and Qwen3 1.7B and 4B
pruned to densities 0.8, 0.7 and 0.6, with accuracy measured by the protocol of
Appendix~\ref{sec:app_probes} on 64 probes per capability, the law's form fitted leave-one-model-out
predicts accuracy with a mean absolute error of 0.145 without calibration and
0.119 with one measurement, whereas the loss route, the final rule's development
median curve followed by a logistic link from loss to accuracy fitted on the other three models, errs by
0.264 and 0.312. Each relation is the better instrument on its own
endpoint: on capability loss the law's form trails the compact power form in every column of
Table~\ref{tab:a14-baselines}, and on accuracy the loss route trails the law, so neither replaces the
other and the paper's claims stay on loss. Two features of the accuracy endpoint explain the
ordering. In 17 of the 36 scored cells the measured accuracy is at most 0.03, a floor that a
collapsing power form in density reaches by construction and that the loss endpoint still resolves,
Gemma 3 1B moving from 3.4 to 10.1 nats on question answering across the same densities; and the link
applied to the measured loss itself errs by 0.147, so a large part of the loss
route's error is the coarseness of accuracy at this probe count rather than the loss prediction.
Where accuracy stays informative the picture reverses in places: Qwen3 4B keeps 0.84 on
mathematics at density 0.8, which the law puts at 0.24 and the loss route at 0.33.
\input{tables/accuracy_comparison}

\subsection{Measured data requirements under distillation}
\label{sec:app_dreq}

The pre-specified experiment behind Fig.~\ref{fig:dreq} trained eighteen trajectories on the 1B and 4B
students, three pool tiers each with three seeds, and evaluated every trajectory at 50, 100 and 200
thousand supervised tokens on six readouts. Table~\ref{tab:dreq-main} lists, for each readout, student and budget at the primary tolerance,
whether the constraint was crossed inside the tested pool range, the crossing interval or the
bound, and the distance of each predictor's recommended pool size from the measured interval; the
supplement gives the full accounting over all tolerances. The boundary relation pre-specified before the run did not predict these intervals better than a
per-student monotone interpolation, which solved the most requests over the same denominator on
the QA distributions and on math and code alike.
\input{tables/dreq_main}

\begin{figure}[tb]
\centering
\includegraphics[width=\textwidth]{figs/dreq_legend.pdf}\\[1pt]
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/dreq_a.pdf}\caption{Fresh 2Wiki}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/dreq_b.pdf}\caption{MuSiQue}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/dreq_c.pdf}\caption{TriviaQA}\end{subfigure}
\caption{Measured data requirements of distillation at a tolerance of 0.25 nats: for each student
and budget, the range of independent tokens between the largest failing and the smallest passing
pool; an upward marker at the largest pool means the constraint is not yet met, a downward marker
at the smallest pool that it is met everywhere, and a cross that the seeds disagree. The largest
pool varies the training seed, and the smaller pools vary which traces are drawn into the pool.}
\label{fig:dreq}
\end{figure}

\section{Mechanisms}
\label{sec:app_mech}

Four analyses develop the mechanism summary of Section~\ref{sec:resp_capabilities}: a pruning
law in the deleted capability mass, the sign of its first-order term, the geometry of the cliff, and
the logit-displacement account of the loss change, in that order.

\subsection{The pruning law}
\label{sec:law}

The mechanism form below fits the full pre-cliff grid.
With the measured alignment term subtracted, the curvature residual
follows a power law in the deleted capability mass $m_c(\dens)$:
\begin{equation}
\Delta\Lc{c}(\dens)\;=\;\underbrace{\gbar{c}^{\top}\dw_\dens}_{\text{measured}}
\;+\;C\,\big[\underbrace{m_c(\dens)}_{\text{measured}}\big]^{\eta},
\label{eq:fitlaw}
\end{equation}
where $\gbar{c}$ is the mean gradient of the capability loss at the dense weights, $\dw_\dens$ the
weight change from pruning to density $\dens$, $C$ a fitted amplitude and $\eta$ a fitted exponent,
with in-sample $R^2 = 0.82$--$0.996$ over 24 capability$\times$model fits. The form is tested on the held-out battery with a 1000-resample cell bootstrap: in \emph{leave-one-family-out} transfer the
shared-$\eta$ mechanism form attains MAE $2.17$ nats versus $10.3$
for per-capability raw sparsity and $8.2$ for the direct-damage baseline, with both intervals excluding zero, so the deleted-mass coordinate
transfers across families; it also beats the direct-damage baseline in fit-shallow/predict-deep and raw sparsity in leave-largest-model-out, while its edge over raw
sparsity within-model does not reach significance; without the first-order term QA is unfittable, and with it QA joins the law. Fig.~\ref{fig:explanation} shows the fitted reuse exponent and the accuracy of the displacement account against the size of the loss change. Two structural results follow. First, in the deleted-mass coordinate the exponent concentrates at
$\eta\in[1.2,2.2]$ across all families, versus $3.2$--$8.7$ in the
raw $(1-\dens)$ coordinate, which makes the form portable. Second, \emph{within a model, $\eta$ is shared across capabilities}: capability differences appear to be carried
by the two measured quantities, and the fitted structure is two
parameters per model. This mechanism form uses gradient
information of the dense model and is not a K0 predictor; it
is reported as an upper comparator for the reduced forms of the main
text and not as a bound on what the initial state can predict.

\subsection{The signed first-order term predicts, in both directions}
\label{sec:mech}

The alignment term $\gbar{c}^{\top}\dw$ is the family
coefficient that carries sign. Its sign predicts the presence or absence of the
capability-selective improvement in each case measured:
the QA term of Qwen3 is negative and QA improves under pruning, with a predicted $-0.83$ against a measured $-0.79$ at 4B and $\dens=0.7$; the QA term of Gemma is
positive and no improvement exists at any size; the term for OLMo-3-7B is positive, a mild degradation predicted and measured at $+0.015$ on code at $\dens=0.8$, while the term for OLMo-3-32B is negative, and
QA improvement under deep pruning appears. At the level of individual model$\times$capability$\times$density
cells in the mild regime, sign prediction accuracy is $67.6\%$ over 108 cells, above chance with a Wilson interval of 58 to 76 percent, and the family-level pattern of Qwen and OLMo-32B QA improvements, none for Gemma and an OLMo size flip was stated in advance. Beyond the mild regime, first- plus second-order
predictions fail by orders of magnitude at the cliff in each family:
the collapse is non-perturbative in loss space, consistent with the
two-regime structure of \eqref{eq:fitlaw}.

\subsection{The cliff is a geometric threshold crossing}
\label{sec:orderparam}

Measuring the capability geometry \emph{of the pruned models}
accounts for the location of the cliff on the three models tested. The capability-region mass retention, the fraction of the capability-exclusive top coordinates of the dense model still prominent after pruning, declines smoothly from the first densities with no cliff in geometry, while loss is still flat;
the behavioral cliff occurs where retention crosses a critical value.
The critical value is family-specific, near 0.04 to 0.10 for Gemma across sizes and capabilities and 0.13 to 0.23 for Qwen, but consistent
within a family across sizes and capabilities: the cliff boundary
$\dens_c^{*}(N_0,f)$, for size $N_0$ and family $f$, reduces to one critical-retention constant per
family, so the fitted cliff parameter of the two-regime
law becomes a measurable order parameter that varies smoothly within each family.
\preliminary{three models, two families; a multi-model extension is left to future work}

\begin{figure}[tb]
\centering
\includegraphics[width=\textwidth]{figs/fig5_legend.pdf}\\[1pt]
\begin{subfigure}[t]{2.7in}\centering\includegraphics[width=\linewidth]{figs/fig5_a.pdf}\caption{1B student}\end{subfigure}\hfill
\begin{subfigure}[t]{2.7in}\centering\includegraphics[width=\linewidth]{figs/fig5_b.pdf}\caption{4B student}\end{subfigure}
\caption{Falsification of the logarithmic reuse term. The pool-change effect divided by its logarithmic bracket against the supervised budget, per capability, for the (a) 1B and (b) 4B students; the heavy line is the per-budget median over pool pairs and the band their range. A logarithmic reuse term would predict a flat line in budget.}
\label{fig:drift}
\end{figure}

\begin{figure}[tb]
\centering
\includegraphics[width=\textwidth]{figs/fig2_legend.pdf}\\[1pt]
\begin{subfigure}[t]{2.7in}\centering\includegraphics[width=\linewidth]{figs/fig2_a.pdf}\caption{Reuse-term curvature}\end{subfigure}\hfill
\begin{subfigure}[t]{2.7in}\centering\includegraphics[width=\linewidth]{figs/fig2_b.pdf}\caption{Displacement account}\end{subfigure}
\caption{Determinants of the distillation response and the displacement account of the compression loss change. (a) The reuse
exponent $p$ across training folds per capability with the full-development value and its
conditional interval; $p=0$ is the logarithmic term and $p=1$ linear. (b) Median relative error of
the second-order displacement account as a function of loss-change magnitude under pruning, grouped quantization and
per-channel quantization.}
\label{fig:explanation}
\end{figure}

\subsection{The logit-displacement account of the compression loss change}
\label{sec:app_disp}

For a compressed model whose logits are displaced by $r$ from those of the reference, the change
in cross-entropy on a supervised token $y$ under the reference next-token distribution $\pi$ is
exactly
\begin{equation}
\Delta L \;=\; \big(\mathbb{E}_{\pi}[r]-r_y\big)\;+\;\tfrac12\,\mathrm{Var}_{\pi}(r)\;+\;O(\lVert r\rVert^3),
\label{eq:displacement}
\end{equation}
so the first-order term is the signed alignment of the displacement with the target and the
second-order term is coefficient-free. Evaluated on 339 cells across pruning, grouped quantization
and per-channel quantization on the development states, the truncation to second order reproduces
the measured loss change with a median relative error that depends on the loss-change magnitude and not on the
family or the knob (Table~\ref{tab:disp_boundary}): under 5\% wherever the loss change is below about
0.1 nats, and rising past 20\% by 0.25 nats. Grouped four-bit quantization with fine groups, at 0.06 nats, sits on the same error curve as per-channel four-bit quantization at 0.24 nats of damage.

\begin{table}[tb]
\normalfont
\centering\small
%
\caption{Median relative error of the coefficient-free second-order account, by family and
configuration. Rows identify compression families; the second column pairs each setting with its median relative error in percent, from a \mbox{post-hoc} analysis of the measured damage.}
\label{tab:disp_boundary}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}>{\raggedright\arraybackslash\hspace{0pt}}p{\dimexpr 0.250000000\textwidth-0.500000000\tabcolsep\relax}>{\raggedright\arraybackslash\hspace{0pt}}p{\dimexpr 0.750000000\textwidth-1.500000000\tabcolsep\relax}@{}}
\toprule
Family & Configuration: median relative error \\
\midrule
Pruning & $d=0.9$: 0.9\%; 0.85: 4.3\%; 0.8: 5.3\%; 0.75: 11.1\%; 0.7: 16.0\%; 0.65: 28.2\%; 0.6: 44.4\% \\
Grouped quantization & 5 bits: 1.6--2.7\% at each group size; 4 bits: 3.3\% to 9.2\% by group size; 3 bits: 35.9\% \\
Per-channel quantization & 8 bits: 2.5\%; 6 bits: 3.0\%; 5 bits: 4.2\%; 4 bits: 23.8\%; 3 bits: 53.9\% \\
\bottomrule
\end{tabular*}
%
\end{table}

Two readings of this account are excluded. Projecting the displacement onto the reference logits $\ell$
with a shift-invariant estimator, $\epsilon=-\mathrm{Cov}_{\pi}(r,\ell)/\mathrm{Var}_{\pi}(\ell)$, since the uncentred version moves by 85\% under a constant logit shift, the pure-shrinkage account
fails on magnitude: about three percent of the response lies in the shrinkage direction, and the
variance of the orthogonal residual carries the rest for math and code. That residual variance is
not shown to be random noise, and it is not a pre-compression predictor, because computing it needs
the compressed model. The account explains the loss change after the fact and enters no
relation as an input. Variance dominance is per capability: seventeen cells with negative loss
change are carried by the first-order term.

\section{Capability-Specific Selection}
\label{sec:app_selection}

\subsection{Independent cross-method validation}
\label{sec:app_s3}

The confirmation of Section~\ref{sec:selection} used four references, none of which supplied a
measurement to the final rule. Table~\ref{tab:s3-identity} gives the exact releases, revisions and prior use, and
separates a new model state from a new intervention on familiar weights. Each reference carried
four pruning densities, six quantization settings, the dense model and one student distilled in
this round under the recipe on which the delivered relation was fitted. Two questions were settled
before any outcome was observed, against two different pre-specified sets. The first is checkpoint
identity: the learned values of all six snapshots were compared with the thirty Pythia revisions in
the earlier records, 180 comparisons in which no snapshot matched any of them. The second is
prior use, which Table~\ref{tab:s3-identity} records: the two Pythia step-120000 states
appear in no earlier record, whereas the Gemma releases were measured in earlier development, so
for them the round tests new candidates on familiar weights. The dense reference and pristine
student losses were then measured on the fixed probes, and 144 capability predictions, 48
largest-increase predictions and both selection maps were fixed before any outcome was observed. Table~\ref{tab:s3-selection} reports the outcome for each reference and objective. Question answering is the only objective with an opportunity to capture, and the rule captures it;
on the other three the opportunity is negligible for every policy.
\input{tables/s3_identity}
\input{tables/s3_selection}

A retrospective control, outside the pre-specified analysis, separates the selection opportunity that an
initial smaller student supplies by itself from the further opportunity associated with the
distillation. Table~\ref{tab:s3-retrospective-control} compares the best feasible loss when
quantization is supplemented with the pristine student against the same quantity when it is
supplemented with the distilled student, at the same nominal storage ratio. Its entries are
therefore differences in candidate-set opportunity, and not the loss change
$L(S_{\mathrm{KD}})-L(S_0)$ of a student measured against its own starting point. The final rule
never saw the pristine candidate, so the original pre-specified selections and the pre-specified results are
unchanged.
\input{tables/s3_control}

\paragraph{Policy ablation.} A retrospective ablation scores three policies, defined after the
round from development data alone, on the same candidates, budgets and outcomes
(Table~\ref{tab:s3-ablation}): a development-fixed method priority with no numeric prediction,
which ranks methods by their oracle win rate on the development panel and takes the largest
feasible storage within the method; development-median predictions for every candidate, with the
development constant for the student; and the source-conditioned relations of
Section~\ref{sec:laws} where their inputs exist, which excludes the two Gemma references without a
disclosed pretraining token count, with the selected distillation forms for the student. Writing
$\widehat Q$ for the quantization-only choice, $Q^*$ for the best quantization candidate and $A^*$
for the best candidate of any method, the quantization-only regret decomposes as
$L(\widehat Q)-L(A^*)=[L(\widehat Q)-L(Q^*)]+[L(Q^*)-L(A^*)]$, the selection error inside the family
plus the opportunity; the last column of the table reports $L(Q^*)-L(\widehat A)$ for the rule's
choice $\widehat A$, the gain that survives a perfect quantization selection. On these references the heuristic matches the rule wherever the opportunity sits in one method, and
the medians add value on mathematics for Gemma 3 1B, where the rule's choice outside quantization is
what the heuristic misses; the numeric value of prediction is thus carried by the development
medians the rule already uses, and the contribution of the source-conditioned relations lies in
measurement efficiency (Section~\ref{sec:efficiency}) rather than in selection.
\input{tables/s3_ablation}

\paragraph{Fresh-item check of the chosen models.} After the round, every model that a policy chose
for question answering, with each reference's dense model and initial student, was scored on 384
2WikiMultihopQA validation items that no probe, sample, teacher trace or few-shot exemplar had used
(Table~\ref{tab:p1-behaviour}). Losses and their differences are token-weighted means in the sense
of Eq.~\ref{eq:taskloss}, the differences bootstrapped over items. The loss endpoint replicates: the
distilled student the rule chose has a lower loss than the quantization-only choice by 0.55, 0.45 and
1.82 nats on Pythia 410M, Pythia 1.4B and Gemma 3 1B, with paired 95\% intervals excluding zero, and
by 0.19 on Gemma 3 4B with an interval that includes zero; against its own initial student it is lower
by 0.37, 1.05 and 0.91 nats on Pythia 1.4B, Gemma 3 1B and Gemma 3 4B and unchanged on Pythia 410M,
whose initial student already carries the advantage, so the initial student separates the part of the
gain that comes from the smaller model from the part that distillation adds. The same items were
then read out by greedy generation, an endpoint the selection never targeted, under two protocols.
The first, the 32-token protocol of Appendix~\ref{sec:app_probes}, scores the whole block before
the next question marker; under it the students, which answer in the teacher's longer style, reach
exact match of 0.029, 0.065, 0.000 and 0.003 against 0.044, 0.167, 0.036 and 0.086 for the
quantization-only choices. The second gives 96 tokens and applies one gold-blind extraction rule
to every model, the first sentence of the first line with markdown removed, and records whether a
generation reached the cap without a stop marker. Under that readout the students score 0.089,
0.091, 0.115 and 0.172, ahead of the quantization-only choice by 0.04, 0.07 and 0.09 on Pythia
410M, Gemma 3 1B and Gemma 3 4B, with paired intervals excluding zero, and behind it by 0.06 on
Pythia 1.4B; against their own initial students they are level on three references and 0.09 behind
on Pythia 1.4B, so the loss gains over the initial student do not convert into exact-match gains.
The students reach the 96-token cap on 63 to 99 percent of items, since they rarely emit a stop
marker: a protocol audit, recorded with the results, found that their training targets carry no
end-of-answer marker, while the stop handling, few-shot format and extraction rule are the same for
every candidate. The shared procedure makes the candidates comparable under one readout; it does not
exclude that the missing end-of-answer supervision itself shapes the students' generations. A target
without an end marker leaves the teacher-forced loss, the endpoint the selection optimises, intact,
and limits how far the generation readout can be read and carried to other distillation protocols;
Appendix~\ref{sec:app_eos} reports a paired control that adds the marker. A reading of 20 sampled items per model, recorded with the results, found the students'
first answer correct on 4, 7, 5 and 6 items against 3, 4, 2 and 5 for the quantization-only
choices, with the difference between correct content and exact match confined to run-on sentences
and initials that the rule now handles; the remaining student errors are wrong answers. The two
readouts disagree by up to 0.14 in exact match on the same models, so generation accuracy is
sensitive to the protocol, and its ordering of the candidates does not follow the loss ordering
on every reference. Reference-answer likelihood and generation correctness are therefore different
endpoints, and which of them a selector optimises decides which model it recommends; the rule of
Section~\ref{sec:selection} optimises the former, and a deployment that needs a particular answer
format or accuracy enters that endpoint as its own objective.
\input{tables/p1_behaviour}

\paragraph{Storage in bytes.} The selection states its budget in nominal storage, the fraction of
stored matrix parameters: the retained density for pruning, $b/16$ plus the 16-bit group scales for
quantization, and the ratio of block-matrix parameter counts for a student.
Table~\ref{tab:storage-accounting} prices the same candidates in bytes on one base, every weight
matrix of the model in 16-bit precision. Grouped quantization is exact and per-channel quantization
is within 0.1 percent of its nominal ratio, since its row scales are few. Unstructured pruning needs
an index: a bitmap adds one sixteenth at every density, and a compressed sparse row format doubles
the cost, so it pays only below density 0.5. A student's bytes exceed its nominal ratio by its
embedding rows, which a small model carries in larger proportion: 0.40 against 0.28 for Pythia 160M
under Pythia 410M, and 0.27 against 0.14 for Gemma 3 270M under Gemma 3 1B. Read as a byte budget,
the same grid changes the feasible set in 40 of the 272 selection cells and the rule's choice in 10,
all of them student choices at budgets from 0.20 to 0.35; on 25 further cells a per-channel choice
exceeds its budget by at most 0.2 percent. Re-running the selection with the feasible sets counted
in bytes, and scoring against the byte-feasible oracle, leaves the picture intact within each family,
native nats never pooled across tokenizers: on question answering the Pythia pair's opportunity is
0.31 nats against 0.34 nominally and the rule's regret 0.050 against 0.014, the difference being the
Pythia 410M student, infeasible in bytes at budgets 0.30 and 0.35, where the rule falls back to
quantization, with the quantization-only regret 0.54 against 0.58; the Gemma pair's opportunity is
0.51 against 0.55, the rule's regret zero under both units and the quantization-only regret 1.05
against 1.12. On mathematics, code and the largest increase the Pythia pair has no opportunity under
either unit, and the Gemma pair's rule regret of 0.008, 0.014 and 0.028 nats nominally falls to zero
in bytes, where the per-channel candidates that sat exactly at their budgets drop out of the
feasible sets. The rule
itself does not depend on the unit: it ranks whatever candidates the budget admits, and a deployment
that stores sparse weights or counts embeddings enters its own byte costs in place of the nominal ones.
\input{tables/storage_accounting}


This section gives the selection rule behind Section~\ref{sec:selection}, its retrospective validation
and its prospective confirmation. Section S6 of the supplement tabulates the per-policy results behind Fig.~\ref{fig:selection}: mean regret against the measured oracle, agreement with the oracle's method, the share of no-clear-winner cells, and the share of cells in which a fixed-method policy cannot meet the budget; such cells are infeasible, have no fallback and enter no mean.
\begin{figure}[tb]
\centering
\includegraphics[width=5.5in]{figs/selection_feasible_legend.pdf}\\[1pt]
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/selection_feasible_a.pdf}\subcaption{Math}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/selection_feasible_b.pdf}\subcaption{Code}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/selection_feasible_c.pdf}\subcaption{QA on 2Wiki}\end{subfigure}

\caption{Capability-specific selection maps on held-out Pythia states under the earlier laws, retrospectively and leave-one-state-out. For each state and nominal storage budget the colour is the method with the lowest predicted loss among feasible measured configurations; grey cross-hatching marks cells with no feasible configuration, diagonal hatching the no-clear-winner heuristic, and a hollow circle with cross-hatching a cell in which the measured oracle chooses another method.}
\label{fig:selection}
\end{figure}

The rule was first validated retrospectively. On the controlled panel of 17 states and budgets from 0.2 to 1, with each law refit with the target held out, the law-based choice is within 0.19, 0.17 and 0.42 nats of
the oracle for math, code and QA and picks the oracle's method in 96\%, 95\% and 73\% of 289 cells;
quantization-only is close behind.


It was then fixed and confirmed on independent states. The rule fixed
before the confirmation panel was measured is as follows:
pruning uses the power form for math and code and the median curve
for QA at unseen densities of seen sizes, and the median development
curve for each capability on new sizes or stages; per-channel
quantization on new states uses the per-bit development median;
grouped quantization uses the same-input piecewise interpolation for
math and code and the per-configuration median for QA on seen states,
and the per-configuration median for each capability on new states;
distillation with a same-stage smaller student uses the student's
reference loss plus a per-capability constant for code and QA or the linear student-state form for math, QA on the 2Wiki distribution only.
Selection minimizes the predicted absolute loss over feasible measured
configurations, or the largest predicted loss increase across
capabilities for the multi-capability objective
(Table~\ref{tab:locked_rule} lists the rule as coded, including where
it departs from the delivered relations of Section~\ref{sec:laws}); quantization-only
uses the same candidates, predictor, and feasibility rule with no
fallback. Predictions under this rule and under the earlier
source-conditioned laws were pre-specified for four fresh states before
measurement (Table~\ref{tab:rule-confirm}; Fig.~\ref{fig:rule_maps});
the pre-specified criterion confirms a capability when the mean regret of the final rule over the 68 cells is within that of the source-conditioned map and below that of quantization-only; math, code, and QA meet it, while the multi-capability objective misses the first condition by 0.0001 nats, 0.0020 against
0.0019, and is reported as prospective and unconfirmed. The distillation candidates on 1B@64k are the
existing students 160M@64k and 410M@64k, whose post-training
outcomes were purged from the fits that predict them; per-state
regrets and candidate-set sizes are in
Section S6 of the supplement; the coverage columns there
describe whether the no-clear-winner candidate set contains the oracle's method or configuration, and the method agreement of the single chosen
configuration is the figure quoted in the main
text.
\input{tables/rule_confirm}
\input{tables/locked_rule}
\begin{figure}[tb]
\centering
\includegraphics[width=5.5in]{figs/rule_regret_legend.pdf}\\[1pt]
\includegraphics[width=5.5in]{figs/rule_regret_a.pdf}
\caption{Mean regret per objective on the four earlier Pythia states, in nats per native token on
a logarithmic scale, for the final rule, the earlier source-conditioned laws, quantization-only
and the cheapest feasible configuration; the saved pooled means of the confirmation round are
plotted without recomputation.}
\label{fig:rule_regret}
\end{figure}
\begin{figure}[tb]
\centering
\includegraphics[width=5.5in]{figs/rule_maps_full_legend.pdf}\\[1pt]
\begin{subfigure}[t]{2.7in}\centering\includegraphics[width=\linewidth]{figs/rule_maps_full_a.pdf}\subcaption{Math}\end{subfigure}\hfill
\begin{subfigure}[t]{2.7in}\centering\includegraphics[width=\linewidth]{figs/rule_maps_full_b.pdf}\subcaption{Code}\end{subfigure}\\[3pt]
\begin{subfigure}[t]{2.7in}\centering\includegraphics[width=\linewidth]{figs/rule_maps_full_c.pdf}\subcaption{QA on 2Wiki}\end{subfigure}\hfill
\begin{subfigure}[t]{2.7in}\centering\includegraphics[width=\linewidth]{figs/rule_maps_full_d.pdf}\subcaption{Largest loss increase}\end{subfigure}

\caption{Full selection maps on the four fresh states under the final rule and under the earlier source-conditioned laws, in row pairs, against nominal storage budget; colour: predicted method; hollow circle with cross-hatching: the oracle differs; diagonal hatching: no clear winner. The last panel uses the largest loss increase across capabilities as the objective.}
\label{fig:rule_maps}
\end{figure}


\subsection{Protocol sensitivity of the generation readout}
\label{sec:app_eos}

The selection-round students were trained on teacher answers with no end-of-answer marker. A token-level
audit, recorded with the results, rebuilt the 1,781 training examples of each of the four students with
the trainer's own functions: no target carries the tokenizer's end-of-sequence token among its
supervised labels, the label mask covers exactly the completion tokens, 1,437 targets end in a sentence
mark and 4 in a newline, and the generation readout stops on the end-of-sequence token and on the
next-question markers for every candidate. To measure what the missing marker does, two students were retrained with one change, the
end-of-sequence token appended to every target and supervised, 1,781 extra supervised tokens per
epoch on the same examples, seed, learning rate and two-epoch schedule: the student of the Pythia
1.4B reference, on which the loss and generation orderings disagree most, and the student of the
Gemma 3 1B reference, which reached the 96-token cap on 99 percent of items.
Table~\ref{tab:eos-control} gives both pairs' readouts on the 384 fresh items. The marker restores
output control on both: the share of generations reaching the cap falls from 0.63 and 0.99 to 0.01
and 0.00, and the mean generation from 64 and 96 tokens to 5 and 8. It leaves the
reference-completion loss where it was, $-0.007$ nats [$-0.060$, $+0.047$] on Pythia and $+0.008$
[$-0.022$, $+0.037$] on Gemma. On generation accuracy the two students differ. The Pythia student's
exact match is unchanged, $+0.018$ [$-0.010$, $+0.049$] at 32 tokens and $0.000$ [$-0.034$,
$+0.031$] at 96; it keeps its loss advantage over the quantization-only choice, $-0.45$ nats, with
its exact-match deficit, $-0.07$ [$-0.11$, $-0.03$], and the same against its initial student,
$-0.38$ nats and $-0.10$. The Gemma student, whose untreated generations never stopped, gains
$+0.120$ [$+0.089$, $+0.154$] at 32 tokens and $+0.044$ [$+0.021$, $+0.068$] at 96, which puts it
ahead of the quantization-only choice on exact match by $0.086$ [$0.049$, $0.122$] as well as on loss
by $1.81$ nats, and level with its initial student on exact match, $-0.003$ [$-0.042$, $+0.036$],
while $1.05$ nats ahead on loss. The missing marker therefore costs generation accuracy where the
untreated student never terminates and nothing where it sometimes does; in neither case does the
loss gain over the initial student convert into an exact-match gain, and on Pythia 1.4B the
disagreement between the loss ordering and the generation ordering stands with the answers properly
terminated. Which endpoint a selector optimises still decides which model it recommends, on either family.
\input{tables/eos_control}

\section{Reproducibility and Timing of Predictions}
\label{sec:app_registration}

The development and confirmation rounds were complete by 11 September 2026; the corner design of
Section~\ref{sec:why} was specified on 14 September, and the measurement-efficiency confirmation of
Section~\ref{sec:efficiency} and the data-requirement experiment of Section~\ref{sec:observed} on 16
September, each before its models were trained or pruned; no measurement taken after a prediction
was fixed enters that prediction or its baseline.
\label{sec:app_register}
\begin{table}[H]
\normalfont
\centering\footnotesize
\caption{Information budgets: K0 uses no compressed-target calibration, K1 one calibration
measurement, and the oracle row is a post-hoc diagnostic.}
\label{tab:budgets}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}>{\raggedright\arraybackslash\hspace{0pt}}p{\dimexpr 0.26\textwidth-1.04\tabcolsep\relax}>{\raggedright\arraybackslash\hspace{0pt}}p{\dimexpr 0.40\textwidth-1.60\tabcolsep\relax}>{\raggedright\arraybackslash\hspace{0pt}}p{\dimexpr 0.34\textwidth-1.36\tabcolsep\relax}@{}}
\toprule
Budget & Information available & Cost to disclose \\
\midrule
Metadata only (K0) & $N_0$, public $D_0$, architecture, intervention configuration & Metadata
availability; $D_0$ may be undisclosed \\
Metadata and dense anchor (K0) & Plus the dense capability losses $L_{0,c}$, measured once before
compression & The probe count, one forward pass per probe, native loss units \\
One target calibration measurement (K1) & Plus one pre-specified measurement of the target at one
compression configuration & The calibration configuration and its compression cost \\
Oracle diagnostic & Rescaling on measured target outcomes & No predictive budget; a post-hoc measure
of form misfit \\
\bottomrule
\end{tabular*}
\end{table}

Prospective tests follow one order: forms, development set, standardization and coefficients are fixed; the reference losses of a new source are measured; each candidate's predictions are recorded with their hash; only then is the outcome measured.

\FloatBarrier
\section{Discussion}
\label{sec:app_learned}

Four lessons from building the relations bear on how such laws should be built and used. A
rejected form is not a rejected class: the logarithmic reuse term failed its own flatness
implication, while a fit-free second-difference test on two students did not reject additivity of
budget and reuse (Section~\ref{sec:why}), so the class stays open to designs that can resolve it.
An explanation is not a predictor: the displacement account reproduces the loss change across
three families but needs the compressed model to compute, which is why the delivered relations
read only what is known before compression. Source information transfers less than it fits: dense
pre-compression statistics improve a parametric family in four of nine pairs and lose to a
source-free configuration curve in nine of nine, which is why the selection rule calls that
curve's medians on every new state. Identifiability is a property of the design: the one fit-free
statistic that separates additive from interacting distillation structures was uncomputable until
four trajectories were designed to land on a budget-by-reuse rectangle. Each relation holds for the
evaluated distributions, implementations and configuration ranges stated with it, and extending the
set to a new compression algorithm, capability or data source is a matter of the same protocol, one
development panel and one pre-specified test, rather than of a new theory.

