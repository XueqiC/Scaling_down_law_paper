\section{Capability Scaling-Down Laws}
\label{sec:laws}


Each method delivers one relation in the interface of Eq.~\ref{eq:interface}, turning the
observations of Section~\ref{sec:observed} into a prediction from the initial state and the configuration.
Each is stated below with its inputs and its fitted and tested domain; Section~\ref{sec:general}
reports how it performs on configurations and model states that entered no fit. Whether the initial state earns its place is decided per capability: on the controlled pruning
panel, a per-setting regression using parameter count, pretraining tokens and reference capability
loss reduces the mean absolute error from 0.41 to 0.25 nats for mathematics and from 0.52 to 0.29
for code, and raises it from 0.50 to 0.81 for question answering.


\begin{itemize}[leftmargin=1.2em,itemsep=2pt,topsep=3pt]
\item \textbf{Pruning.} Mathematics and code take the power form of Eq.~\ref{eq:power} from the
parameter count, the pretraining tokens and the reference loss, at unseen densities in $[0.6,0.9]$
of a state in the fit and at new training stages of sizes inside the fitted range (confirmed on four
such states, Section~\ref{sec:efficiency}). At sizes
outside that range, and for question answering, the delivered relation is the pre-specified development
median curve $L_{0,c}+\widetilde f_c(d)$, which reads only the reference loss. Fitted and tested on Pythia 160M--1.4B at three
pretraining stages, densities 0.6--0.9 and tests to 0.55.
\item \textbf{Quantization.} Per-channel quantization on a new state takes the per-bit development
median. Grouped quantization on a state in the fit takes a same-input piecewise interpolation over
the measured bit-width and group-size grid, linear in $\log_2 g$, for mathematics and code; question
answering, and every capability on a new state, takes the per-configuration median. Fitted and
tested on the same Pythia states over 3--8 bits and groups of 32--512 weights.
\item \textbf{Distillation.} The signed transfer response of Section~\ref{sec:loss} takes
$\widehat\delta_c=a_c\log(1+E)$ for mathematics and code and the joint budget-pool form for question
answering, from the supervised tokens, the unique-pool tokens and the student's own reference loss.
Fitted and tested on Gemma-3 270M and 1B students under one recipe, with pools to 158k tokens and
budgets of 50k--200k, and question answering scored on 2Wiki.
\end{itemize}

\subsection{Pruning}
\label{sec:law_prune}

Within the controlled pruning domain, we model the response with the compact form
\begin{equation}
\widehat{\Delta L}_c(\mathbf x,d)=A_c(\mathbf x)\,\Big(\frac{1-d}{0.3}\Big)^{\gamma_c},
\qquad
A_c(\mathbf x)=\beta_c\cdot\big(1,\tilde{\log N_0},\tilde L_{0,c},\tilde{\log D_0}\big),
\label{eq:power}
\end{equation}
This form uses five parameters per capability, with inputs standardized on the development set, and
Section~\ref{sec:efficiency} evaluates what it costs to fit. Its advantage holds at new stages of
sizes inside the fitted range but not under broader model-state transfer, so at sizes outside the
range and for question answering we deliver the source-free development median; the selection rule
of Section~\ref{sec:selection} calls that median on every new state, as its conservative default.
\subsection{Quantization}
\label{sec:law_quant}

Per-channel round-to-nearest quantization is evaluated at discrete bit widths, and prediction is
evaluated separately across bit widths and model states. At a bit width that entered the fit,
the delivered relation is the per-bit source regression on states in the fit and the per-bit median
on new ones. With
groups of $g$ weights per scale as a second axis, the delivered relation for mathematics and code on seen
states is a piecewise interpolation over the measured $(b,g)$ grid, linear in $\log_2 g$; for question answering, and for every capability at a new state, it is the per-configuration
development median measured on the same grid. 
\subsection{Distillation}
\label{sec:law_distill}

The response is the signed transfer response of Section~\ref{sec:loss}, parameterized by supervised
completion tokens, unique pool tokens and their ratio; the teacher supplies the training targets and
is fixed within the evaluated protocol. The delivered relations are
\begin{equation}
\widehat\delta_c=a_c\log(1+E)\ (\text{math, code}),\qquad \widehat\delta_{\mathrm{QA}}=u\,(a+b\,u+q\,v),
\label{eq:kdforms}
\end{equation}
with $u=\log(1+T/T_{\mathrm{ref}})$ and $v=\log(D_U/D_{\mathrm{ref}})$ for reference constants fixed on
the development set: one parameter for mathematics and code, which vanish at zero budget, and three for question
answering. On the development students at the 2Wiki distribution, question answering improves under
every development recipe while mathematics and code move by hundredths to tenths of a nat. At high
reuse, and on the other question-answering distributions, the response reverses
(Section~\ref{sec:observed}).
The distillation relations describe the 270M and 1B students at the evaluated budgets and reuse
counts, not the 4B student or reuse near fourteen, and the question-answering result is specific to
2Wiki. A joint relation that predicts both budget and data-pool changes reliably remains unresolved.
Section~\ref{sec:general} evaluates these relations on configurations and model states that entered no
fit, and measures the development budget each one needs.

