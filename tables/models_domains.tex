\begin{table}[t]
\setbox2=\vbox{\begin{minipage}{\linewidth}\centering\footnotesize\setlength{\tabcolsep}{3pt}
\setbox0=\hbox{
\begin{tabular}{@{}p{1.9cm}p{3.1cm}p{1.7cm}p{2.4cm}p{1.5cm}p{2.2cm}p{1.9cm}@{}}
\toprule
Arm & Predictor & Parameters per capability & Varied setting; development states & Target calibration & Tested range & Coefficients \\
\midrule
Pruning & $A_c(\mathbf x)\,((1-d)/0.3)^{\gamma_c}$ (Eq.~\ref{eq:power}); same-input alternative A2 (per-$d$ regression + interpolation) & 5 (A2: 20) & $d$; 17 Pythia states (160M--6.9B, 16k--143k) & 0 & $d\in[0.55,0.9]$ interpolation on new states; $d=0.55$ extrapolation fails & Table~\ref{tab:round3_coef} \\
Quantization, fixed bit width & per-bit regression $\beta_{c,b}\cdot\phi(\mathbf x)$ & 4 per $b$ & new state at seen $b\in\{8,6,4,3\}$; 9 development states & 0 & seen $b$; 5-bit rule fails; the near-zero regime is uninformative & register v38 and v49 \\
Quantization, bit width and group size & $\phi(\mathbf x)\cdot[1,u,v,uv,u^2]$ (Eq.~\ref{eq:quant2d}) & 20 & $b\in\{3,5\}\times g\in\{64,256\}$; 6 development states & 0 & unseen $b=4$, $g=128$ on development states; mixed results on the new state & Table~\ref{tab:quant2d_coef} \\
Distillation, source axis & $\delta_c=\beta_c\cdot\phi(\mathbf x)$ (initial student) & 4 & 9 Pythia students, a fixed pool and budget & 0 & \mbox{held-out} stage; Math only & register v39 \\
Distillation, pool axis & $a_c+b_c\log E$ (and constant, $\log T$, joint) & 2 & Gemma-3-1B; pools of 75 and 600 traces per domain & 0 & an unseen pool of 225 traces per domain: QA only; multi-student predictions frozen before testing & register v41 and v50 \\
\bottomrule\end{tabular}
}\ifdim\wd0>\linewidth\resizebox{\linewidth}{!}{\box0}\else\box0\fi
\caption{The table summarizes predictive models and their domains. Inputs are always $\mathbf x=(N_0,D_0,L_{0,c})$ through $\phi(\mathbf x)=[1,\tilde{\log N_0},\tilde L_{0,c},\tilde{\log D_0}]$; no prediction uses a measurement of the compressed target (target calibration count 0). Ranges summarize \S\ref{sec:general}; coefficients and standardization constants are in Appendix~\ref{sec:app_coef}. Rows identify prediction families; columns give predictors, parameter counts per capability, development settings, target calibration counts, tested ranges, and coefficient sources. Loss responses are in nats per native token; density and reuse are dimensionless, bit widths are in bits, and budgets are in tokens. This is a \mbox{post-hoc} summary of development fits and frozen prediction tests. A2 denotes per-density regression with interpolation; the numbered registers identify the original experiment records.}
\label{tab:models}\end{minipage}}
\centering\ifdim\dimexpr\ht2+\dp2\relax>.95\textheight\resizebox*{!}{.95\textheight}{\box2}\else\box2\fi
\end{table}
