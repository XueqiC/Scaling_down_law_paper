\section{Final Predictive Relations per Method}
\label{sec:laws}


Each arm delivers one relation in the interface of Eq.~\ref{eq:interface}; selection and
boundary detail is in Appendix~\ref{sec:app_selbound}. The anchor $L_{0,c}$ reconstructs an
absolute loss and the response $\widehat\Delta_{m,c}$ is the predictive claim. On the nine-state
panel a per-setting regression on $(N_0,D_0,L_{0,c})$ lowers the pruning error from 0.41, 0.52 and
0.50 nats to 0.25, 0.29 and 0.81 for math, code and QA (Appendix~\ref{sec:app_conditioning}); the
source-conditioned forms below use those inputs, and every arm also carries a source-free relation
for where they add nothing.

\input{tables/main_prediction_v2}

\subsection{Pruning}
\label{sec:law_prune}

\paragraph{Relation.} For a seen source state (size and stage in the fit) at an unseen density in the development range, the compact form
\begin{equation}
\widehat{\Delta L}_c(\mathbf x,d)=A_c(\mathbf x)\,\Big(\frac{1-d}{0.3}\Big)^{\gamma_c},
\qquad
A_c(\mathbf x)=\beta_c\cdot\big(1,\tilde{\log N_0},\tilde L_{0,c},\tilde{\log D_0}\big),
\label{eq:power}
\end{equation}
with five parameters per capability (tildes standardized on the
development set) approximates the twenty-parameter per-density regression A2 for math and code; for any new state (a new size, or a new stage of a seen size, which takes precedence) and for QA everywhere, the delivered relation is the source-free median development curve,
$\widehat L_c=L_{0,c}+\widetilde f_c(d)$.
\subsection{Quantization}
\label{sec:law_quant}

\paragraph{Relation.} Per-channel round-to-nearest offers no continuous axis (int8 and int6 within
0.01 nats of the reference, int4 0.03 to 0.35, int3 collapse); at a seen bit-width the delivered
relation is the per-bit source regression on seen states and the per-bit median on new ones. With
groups of $g$ weights per scale as a second axis, the delivered relation for math and code on seen
states is a same-input piecewise interpolation over the measured $(b,g)$ grid, linear in $\log_2 g$,
with a zero floor beyond it; for QA, and for every capability on a new state, the per-configuration
development median under the same rule.
\subsection{Distillation}
\label{sec:law_distill}

\paragraph{Relation.} The response is the signed transfer $\delta_c$ of
an existing student relative to its own reference loss; the teacher
never enters it, and the coordinates are the supervised completion
tokens $T$, the pool size $D_U$, and the reuse count $E=T/D_U$. The
delivered relations, frozen for the confirmation, are
\begin{equation}
\widehat\delta_c=a_c\log(1+E)\ (\text{math, code}),\qquad \widehat\delta_{\mathrm{QA}}=u\,(a+b\,u+q\,v),
\label{eq:kdforms}
\end{equation}
with $u=\log(1+T/T_{\mathrm{ref}})$ and $v=\log(D_U/D_{\mathrm{ref}})$: one parameter for math and code, which vanish at zero budget, and three for QA; QA improves under every recipe we ran while math and
code move by hundredths to tenths of a nat.
\paragraph{What a practitioner can compute today.} Table~\ref{tab:main_prediction_v2} gives one relation per arm and task with its range and its
error against the development-stage strongest baseline. For pruning at an unseen density of a
seen source, the power form predicts math and code to about 0.24 nats and should not be used for QA
or outside $d\in[0.6,0.9]$. For quantization the measured grid is the predictor: interpolate group
sizes on a seen state, use the per-bit median on a new state, and expect 0.13 to 0.17 nats. For
distillation, the one-coefficient reuse forms and the QA joint form hold on the development students
at the 2Wiki distribution; the budget-doubling and data-change interventions are not predicted by any
of the parsimonious forms tried, and the corresponding rows of the table say so. Each row carries its
status: development, frozen prediction, or post-hoc recommendation.
