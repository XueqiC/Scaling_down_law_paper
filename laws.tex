\section{Final Predictive Relations per Method}
\label{sec:laws}


Each method delivers one relation in the interface of Eq.~\ref{eq:interface}, and
Table~\ref{tab:main-prediction-v2} collects the frozen predictions by method and task with the
range on which each was tested, its error and the baseline selected inside the development folds.
Development results and recommendations made after a test are identified as such in the text. On the controlled pruning panel, a per-setting regression using parameter count, pretraining tokens, and reference capability loss reduces the mean absolute error from 0.41 to 0.25 nats for math and from 0.52 to 0.29 for code, but increases it from 0.50 to 0.81 for QA. We therefore assess the value of model-state inputs separately for each capability and prediction setting.


\input{tables/main_prediction_v2}

\subsection{Pruning}
\label{sec:law_prune}

Within the controlled pruning domain, we model the response with the compact form
\begin{equation}
\widehat{\Delta L}_c(\mathbf x,d)=A_c(\mathbf x)\,\Big(\frac{1-d}{0.3}\Big)^{\gamma_c},
\qquad
A_c(\mathbf x)=\beta_c\cdot\big(1,\tilde{\log N_0},\tilde L_{0,c},\tilde{\log D_0}\big),
\label{eq:power}
\end{equation}
This form uses five parameters per capability, with inputs standardized on the development set. \S\ref{sec:efficiency} evaluates its measurement efficiency on four held-out model states: fitting 18 development measurements yields math and code errors within 0.013 and 0.020 nats of the per-density regression fitted on 36 measurements. Its advantage is not consistent under broader model-state transfer. For those settings, and for QA, the source-free development median remains a strong baseline and the default delivered predictor.
\subsection{Quantization}
\label{sec:law_quant}

Per-channel round-to-nearest quantization is evaluated at discrete bit widths. Responses range from near-zero changes at higher precision to large, state-dependent damage at low precision. We therefore evaluate prediction separately across bit widths and model states. At a bit-width that entered the fit,
the delivered relation is the per-bit source regression on states in the fit and the per-bit median
on new ones. With
groups of $g$ weights per scale as a second axis, the delivered relation for mathematics and code on seen
states is a piecewise interpolation over the measured $(b,g)$ grid, linear in $\log_2 g$; for question answering, and for every capability at a new state, it is the per-configuration
development median measured on the same grid. 
\subsection{Distillation}
\label{sec:law_distill}

The response is the signed loss change of a trained student relative to its own initial checkpoint. The teacher supplies the training targets and is held fixed within the evaluated protocol. We parameterize the response by supervised completion tokens, unique pool tokens, and their ratio, the reuse count. The delivered relations are
\begin{equation}
\widehat\delta_c=a_c\log(1+E)\ (\text{math, code}),\qquad \widehat\delta_{\mathrm{QA}}=u\,(a+b\,u+q\,v),
\label{eq:kdforms}
\end{equation}
with $u=\log(1+T/T_{\mathrm{ref}})$ and $v=\log(D_U/D_{\mathrm{ref}})$ for reference constants fixed on
the development set: one parameter for mathematics and code, which vanish at zero budget, and three for question
answering. On the development students at the 2Wiki distribution, question answering improves under
every development recipe while mathematics and code move by hundredths to tenths of a nat. At high
reuse, and on the other question-answering distributions, the response reverses
(\S\ref{sec:observed}).
Table~\ref{tab:main-prediction-v2} distinguishes frozen test predictions from the predictors recommended after evaluating the accumulated evidence. The compact pruning form supports measurement-efficient prediction within the controlled domain, including the held-out states in \S\ref{sec:efficiency}. Quantization uses configuration-based interpolation or development medians according to the prediction setting. The distillation relations describe the evaluated students and budget ranges, with QA results specific to 2Wiki. A joint relation that reliably predicts both budget changes and data-pool changes remains unresolved.

