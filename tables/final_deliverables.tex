\begin{table}[p]
\setbox2=\vbox{\begin{minipage}{\linewidth}
\centering\scriptsize\setlength{\tabcolsep}{3pt}\renewcommand{\arraystretch}{1.05}
\setbox0=\hbox{
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{1.8cm}>{\raggedright\arraybackslash}p{3.2cm}>{\raggedright\arraybackslash}p{2.3cm}>{\raggedright\arraybackslash}p{2.8cm}>{\raggedright\arraybackslash}p{2.7cm}>{\raggedright\arraybackslash}p{2.35cm}@{}}
\toprule
Arm & Delivered predictor & Inputs & Prediction and rule status & Gain over strongest same-information baseline & Not applicable to \\
\midrule
Pruning & Seen source states (size and stage in the fit), unseen $d\in[0.6,0.9]$, Math and Code: power form of Eq.~\ref{eq:power} or the per-density regression A2. New sizes or stages, and QA: $L_{0,c}+\widetilde f_c(d)$ with the frozen development median curve (rule fixed after the confirmations). & $N_0$, $D_0$, $L_{0,c}$ (source forms); $L_{0,c}$ only (median curve) & Every confirmation prediction frozen; the rule ``median for new sizes'' is retrospective & Math at interpolated densities on Pythia-1.4B at step 112k and Pythia-6.9B at step 80k; no gain for QA; on 2.8B the median curve is 3--7$\times$ better than both source forms & $d\le0.55$; QA source terms; source-conditioned forms on new sizes \\
\addlinespace
Quantization & Seen bit-widths on new states: per-bit development median (the per-bit source regression improved on it only in the leave-one-state-out development evaluation; the selection rule of Appendix~\ref{sec:app_selection} uses the same median). Grouped round-to-nearest quantization on seen states: same-input piecewise interpolation over the measured $(b,g)$ grid with a linear boundary rule in $\log_2 g$ (Math, Code); per-configuration median (QA, new states). & Measured development grid; $N_0$, $D_0$, $L_{0,c}$ for the interpolation & Candidates frozen; the combined rule is retrospective (Appendix~\ref{sec:app_results}) & Interpolation at $g\in\{32,512\}$ on seen states: 0.07, 0.12 against 0.22, 0.31 (surface) and 0.50, 0.56 (median); no gain over the median on the new state & The parametric surface; source conditioning on new states; QA \\
\addlinespace
Distillation & On development students: $\widehat\delta_c=a_c\log(1+E)$ (Math, Code) and the joint budget-pool form (QA); reference is the initial student. & $T$, $D_U$ ($E=T/D_U$); $L_{0,c}$ of the student & Candidates frozen before every test; the headline rule was chosen \mbox{post-hoc} (first test) or \mbox{pre-committed} (confirmation) & Code at a new pool size and budgets on both students ($+0.004$, $+0.005$ nats); QA on 270M ($+0.09$); Math: the baseline performs better & Student size (4B); QA outside the 2Wiki distribution; reuse counts near 14 \\
\bottomrule
\end{tabular}
}\ifdim\dimexpr\ht0+\dp0\relax>.65\textheight\setbox0=\hbox{\resizebox*{!}{.65\textheight}{\box0}}\fi\ifdim\wd0>\linewidth\resizebox{\linewidth}{!}{\box0}\else\box0\fi
\caption{Each compression family is summarized by its predictor, inputs, evidence status, gains, and scope. Errors in nats per token; ``strongest same-information baseline'' is fixed from development before the corresponding test. Rows identify compression families; columns give delivered predictors, inputs, prediction and rule-selection status, observed gains, and excluded settings. This is a \mbox{post-hoc} summary of frozen prediction evidence; A2 denotes per-density regression with interpolation.}
\label{tab:final}
\end{minipage}}
\centering\ifdim\dimexpr\ht2+\dp2\relax>.95\textheight\resizebox*{!}{.95\textheight}{\box2}\else\box2\fi
\end{table}
