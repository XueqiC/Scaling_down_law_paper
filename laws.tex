\section{Final Predictive Relations per Method}
\label{sec:laws}


Each arm delivers one relation in the interface of Eq.~\ref{eq:interface}, and
Table~\ref{tab:main-prediction-v2} collects the frozen predictions by method and task with the
relation and its parameter count, the range it was tested on, its error, and the baseline selected
inside the development folds; development results and post-hoc recommendations are identified as
such in the text. Conditioning on the initial state is justified on the pruning panel, where a per-setting
regression on $(N_0,D_0,L_{0,c})$ lowers the error from 0.41, 0.52 and 0.50 nats to 0.25, 0.29 and
0.81 for math, code and QA; each arm also carries a source-free relation for the cases in which those inputs add nothing.

\input{tables/main_prediction_v2}

\subsection{Pruning}
\label{sec:law_prune}

For a source state in the fit at an unseen density in the development range, the compact form
\begin{equation}
\widehat{\Delta L}_c(\mathbf x,d)=A_c(\mathbf x)\,\Big(\frac{1-d}{0.3}\Big)^{\gamma_c},
\qquad
A_c(\mathbf x)=\beta_c\cdot\big(1,\tilde{\log N_0},\tilde L_{0,c},\tilde{\log D_0}\big),
\label{eq:power}
\end{equation}
with five parameters per capability, tildes standardized on the development set, approximates the twenty-parameter per-density regression for math and code; for any new state and for QA
everywhere the delivered relation is the source-free median development curve
$\widehat L_c=L_{0,c}+\widetilde f_c(d)$.
\subsection{Quantization}
\label{sec:law_quant}

Per-channel round-to-nearest offers no continuous axis: int8 and int6 stay within 0.01 nats of
the reference, int4 costs 0.03 to 0.35 nats and int3 collapses. At a seen bit-width the delivered relation is the
per-bit source regression on seen states and the per-bit median on new ones. With
groups of $g$ weights per scale as a second axis, the delivered relation for math and code on seen
states is a piecewise interpolation over the measured $(b,g)$ grid, linear in $\log_2 g$; for QA,
and for each capability on a new state, it is the per-configuration development median. 
\subsection{Distillation}
\label{sec:law_distill}

The response is the signed transfer $\delta_c$ of
an existing student relative to its own reference loss; the teacher
does not enter it, and the coordinates are the supervised completion
tokens $T$, the pool size $D_U$, and the reuse ratio $E=T/D_U$. The delivered relations, frozen before the confirmation round and written in these coordinates, are
\begin{equation}
\widehat\delta_c=a_c\log(1+E)\ (\text{math, code}),\qquad \widehat\delta_{\mathrm{QA}}=u\,(a+b\,u+q\,v),
\label{eq:kdforms}
\end{equation}
with $u=\log(1+T/T_{\mathrm{ref}})$ and $v=\log(D_U/D_{\mathrm{ref}})$ for reference constants fixed on
the development set: one parameter for math and code, which vanish at zero budget, and three for QA; on the development students at the 2Wiki distribution QA improves under each development recipe
and math and code move by hundredths to tenths of a nat; at high reuse and on other QA
distributions the response reverses (\S\ref{sec:observed}).
In practice, Table~\ref{tab:main-prediction-v2} separates the frozen candidates from the delivered
rules: for pruning the power form is delivered only for unseen densities of the states in its fit
and the source-free median curve for new states; for quantization the measured grid is the
predictor; for distillation the reuse forms and the QA joint form hold on the development students
at the 2Wiki distribution, and the budget-doubling and data-change interventions remain open
targets.
