\begin{table}[H]
\centering\scriptsize\setlength{\tabcolsep}{3pt}\renewcommand{\arraystretch}{1.05}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{1.35cm}>{\raggedright\arraybackslash}p{3.2cm}>{\raggedright\arraybackslash}p{2.3cm}>{\raggedright\arraybackslash}p{2.6cm}>{\raggedright\arraybackslash}p{2.7cm}>{\raggedright\arraybackslash}p{2.35cm}@{}}
\toprule
Arm & Delivered predictor & Inputs & Frozen / retrospective & Gain over strongest same-information baseline & Not applicable to \\
\midrule
Pruning & Seen source states (size and stage in the fit), unseen $d\in[0.6,0.9]$, math and code: power form of Eq.~\ref{eq:power} or the per-density regression A2. New sizes or stages, and QA: $L_{0,c}+\widetilde f_c(d)$ with the frozen median development curve (rule fixed after the confirmations). & $N_0$, $D_0$, $L_{0,c}$ (source forms); $L_{0,c}$ only (median curve) & Every confirmation prediction frozen; the rule ``median for new sizes'' is retrospective & Math at interpolated densities on 1.4B@112k and 6.9B@80k; none for QA; on 2.8B the median curve is 3--7$\times$ better than both source forms & $d\le0.55$; QA source terms; source-conditioned forms on new sizes \\
\addlinespace
Quantization & Seen bit-widths on new states: per-bit development median (the per-bit source regression improved on it only in the leave-one-state-out development evaluation; the selection rule of Appendix~\ref{sec:app_selection} uses the same median). Grouped RTN on seen states: same-input piecewise interpolation over the measured $(b,g)$ grid with a linear boundary rule in $\log_2 g$ (math, code); per-configuration median (QA, new states). & Measured development grid; $N_0$, $D_0$, $L_{0,c}$ for the interpolation & Candidates frozen; the combined rule is retrospective (Appendix~\ref{sec:app_results}) & Interpolation at $g\in\{32,512\}$ on seen states: 0.07, 0.12 against 0.22, 0.31 (surface) and 0.50, 0.56 (median); none over the median on the new state & The parametric surface; source conditioning on new states; QA \\
\addlinespace
Distillation & On development students: $\widehat\delta_c=a_c\log(1+E)$ (math, code) and the joint budget-pool form (QA); reference is the initial student. & $T$, $D_U$ ($E=T/D_U$); $L_{0,c}$ of the student & Candidates frozen before every test; headline rule post hoc (first test) or pre-committed (confirmation) & Code at a new pool size and budgets on both students ($+0.004$, $+0.005$ nats); QA on 270M ($+0.09$); math: baseline better & Student size (4B); QA outside the 2Wiki distribution; reuse counts near 14 \\
\bottomrule
\end{tabular}
\caption{What each arm delivers, answering the same five questions. Errors in nats per token; ``strongest same-information baseline'' is fixed from development before the corresponding test.}
\label{tab:final}
\end{table}
