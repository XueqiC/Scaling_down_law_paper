\section{Empirical Analysis of Capability Loss}
\label{sec:observed}

This section describes the response structure that the predictors of Section~\ref{sec:laws} build
on, measured on the heterogeneous panel and the controlled Pythia and Gemma panels of
Section~\ref{sec:models}. Section~\ref{sec:resp_capabilities} shows which response shapes pruning and
quantization share across capabilities and model states, and Section~\ref{sec:why} shows how training
data, exposure and the evaluation distribution shape the response to distillation and what that asks
of its predictors.

\subsection{Responses to Pruning and Quantization}
\label{sec:resp_capabilities}\label{sec:state_config}

Compression changes capabilities unevenly and in no fixed order (Fig.~\ref{fig:heterogeneity}), which is
the first structure a predictor has to capture. On the controlled Pythia panel, mathematics and code
share one shape in density with a capability-specific scale, whereas the question-answering response
changes sign across density at every state (Fig.~\ref{fig:pythia_responses}). The two
compression axes also carry different shapes. Under pruning, once the measured first-order alignment
term is subtracted, the residual before the cliff is a power law in the deleted capability mass, with an
exponent that transfers across families. Under quantization the response is instead a threshold in bit
width whose position depends on the group size, so an interpolation over the measured grid predicts it
where a smooth surface does not. Fig.~\ref{fig:pythia_responses} shows both shapes on the development
states: pruning losses rise steeply once density falls below 0.8, per-channel losses are large at 3 bits
and small from 4 bits on, and at 3 bits the grouped response grows with the group size while at 4 and 5
bits it stays near zero. Both accounts read measurements of the compressed model, so they explain rather than predict
(Appendix~\ref{sec:app_mech}).

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figs/fig8_legend.pdf}\\[1pt]
\begin{subfigure}[t]{2.7in}\centering\includegraphics[width=\linewidth]{figs/fig8_a.pdf}\caption{Pruning at $d=0.7$}\end{subfigure}\hfill
\begin{subfigure}[t]{2.7in}\centering\includegraphics[width=\linewidth]{figs/fig8_b.pdf}\caption{Per-channel int4}\end{subfigure}
\caption{Capability loss change from each model's dense loss across twelve public models, under (a) pruning to $d=0.7$ and (b) per-channel int4. The ordering of capabilities changes across series and the magnitudes differ by two orders of magnitude at the same setting.}
\label{fig:heterogeneity}
\end{figure}
Model state sets the scale of the pruning response for mathematics and code, and the quantization response depends on the full configuration. On Pythia checkpoints whose size and training stage vary independently, parameter count and
pretraining tokens enter the pruning amplitude for mathematics and code and carry those capabilities to unseen densities of states that
entered the fit; the same inputs over-predict question answering, whose density response is not
proportional to any shared curve. A per-setting regression on parameter count, pretraining tokens and reference loss
reduces the mean absolute error from 0.41 to 0.25 nats for mathematics and from 0.52 to 0.29
for code, and raises it from 0.50 to 0.81 for question answering. Under quantization the configuration acts through two axes at once, the threshold in bit width
moving with the group size, so a relation needs the configuration in full and the initial state only
where it helps. Because the pruning shape is shared across states, a predictor can learn a state's scale
from a few densities and take the shape from the others, which is the measurement saving that
Section~\ref{sec:efficiency} tests.

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figs/fig4_legend.pdf}\\[1pt]
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/fig4_a.pdf}\caption{Pruning}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/fig4_b.pdf}\caption{Per-channel quantization}\end{subfigure}\hfill
\begin{subfigure}[t]{1.8in}\centering\includegraphics[width=\linewidth]{figs/fig4_c.pdf}\caption{Grouped quantization}\end{subfigure}
\caption{Compression responses on the controlled Pythia panel, the structure the predictors of Section~\ref{sec:laws} build on. (a) Loss change against retained density on the nine development states, with the median predictions as heavy lines: mathematics and code rise along one shape with a state-dependent scale. (b) Loss change against bit-width under per-channel round-to-nearest. (c) Loss change against group size at 3, 4 and 5 bits for one state; groups 32 and 512 were unseen when the predictions were pre-specified.}
\label{fig:pythia_responses}
\end{figure}

\subsection{Effects of Training Data and Evaluation Distribution}
\label{sec:why}

The cost of heavy data reuse recurs across question-answering distributions, while the net benefit of
distillation depends on the evaluation distribution. At a matched budget $T$ near 200k supervised tokens
(Fig.~\ref{fig:responses_v2}), a reuse ratio $E=T/D_U$ near twelve costs question answering $+0.29$,
$+2.55$ and $+4.85$ nats on students of 100M, 700M and 3.2B non-embedding parameters, and nine times
more independent data converts the same budget into a gain near one nat on each. Rescored on a fresh
2Wiki sample, on MuSiQue and on TriviaQA at the same budget, all three distributions worsen at high
reuse and improve as reuse falls, but only the fresh 2Wiki sample crosses into a gain. The reuse response also fixes an empirical data requirement: in a pre-specified experiment of eighteen
trajectories on the 1B and 4B students (Appendix Fig.~\ref{fig:dreq}), the independent data at which a
loss constraint is met lies inside the tested pools on the fresh 2Wiki sample for both students at the
two larger budgets, and we report it as measured intervals.

The training recipe is a further condition to consider when extending these relations. With every
other setting held fixed (Appendix Fig.~\ref{fig:lr_pilot}), the loss increase on mathematics and code
grows by an order of magnitude between learning rates of $5\times10^{-5}$ and $1\times10^{-4}$; at the
lower rate the 1B student is almost unharmed on those two capabilities and question answering improves
by $0.79$ nats, so the responses of Fig.~\ref{fig:responses_v2} are measured under one fixed recipe.
These responses motivate predictors that account jointly for data availability and training exposure,
with student state and evaluation distribution determining their range of transfer.
