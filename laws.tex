\section{Capability Scaling-Down Laws}
\label{sec:laws}

Each predictor is built from the response structure of Section~\ref{sec:observed}: the observed
structure fixes the form, the inputs of Section~\ref{sec:models} fill it, and a validation target
decides how Section~\ref{sec:general} judges it. The initial state enters a relation only for
capabilities where it lowered development error (Section~\ref{sec:state_config}).

\subsection{Pruning}
\label{sec:law_prune}

Mathematics and code share one density shape whose scale depends on the model state
(Section~\ref{sec:state_config}), so we write their response as a state-dependent amplitude times a
density term shared across states,
\begin{equation}
\widehat{\Delta L}_c(\mathbf x,d)=A_c(\mathbf x)\,\Big(\frac{1-d}{0.3}\Big)^{\gamma_c},
\qquad
A_c(\mathbf x)=\beta_c\cdot\big(1,\tilde{\log N_0},\tilde L_{0,c},\tilde{\log D_0}\big),
\label{eq:power}
\end{equation}
with five parameters per capability and inputs standardized on the development set. Because the
density term is shared, a development grid can keep every state while measuring fewer densities per
state, and the validation target is the error at half the development measurements against the
full-grid regression on states in no fit (Section~\ref{sec:efficiency}). The form is fitted on Pythia
160M--1.4B at three pretraining stages over densities 0.6--0.9 and holds at new stages of sizes inside
that range. At sizes outside it, and for question answering, whose density response follows no
shared curve, the predictor is the pre-specified development median curve
$L_{0,c}+\widetilde f_c(d)$, which reads only the reference loss and which the selection rule of
Section~\ref{sec:selection} calls on every new state. On another family or pruning criterion the form
and its fitting strategy are reused and the coefficients are estimated anew (Section~\ref{sec:efficiency}).

\subsection{Quantization}
\label{sec:law_quant}

Under quantization the response is a threshold in bit width whose position moves with the group size
(Section~\ref{sec:resp_capabilities}), so the predictors work on the measured configuration grid
rather than on a smooth surface. Per-channel quantization is predicted per bit width, by the per-bit
source regression on states in the fit and the per-bit median on new ones. Grouped quantization adds
the group size $g$ as a second axis: for mathematics and code on seen states the relation is a
piecewise interpolation over the measured $(b,g)$ grid, linear in $\log_2 g$, and otherwise it is the
per-configuration development median. Both are fitted on the Pythia states over 3--8 bits and groups
of 32--512 weights and validated at unseen group sizes of seen states and on a new state
(Sections~\ref{sec:unseen_settings} and~\ref{sec:unseen_states}). Here a complete description of the
configuration and coverage of its grid matter more than a parametric form, and the configuration
medians are the strong baseline that any quantization relation has to beat.

\subsection{Distillation}
\label{sec:law_distill}

Predicting the loss after distillation requires separating the amount of independent data, the
cumulative training exposure and the evaluation distribution (Section~\ref{sec:why}). The relations
therefore read supervised completion tokens $T$, unique pool tokens $D_U$ and their ratio, and predict
the signed transfer response of Section~\ref{sec:loss}; the teacher supplies the training targets and
is fixed within the evaluated protocol. They are
\begin{equation}
\widehat\delta_c=a_c\log(1+E)\ (\text{math, code}),\qquad \widehat\delta_{\mathrm{QA}}=u\,(a+b\,u+q\,v),
\label{eq:kdforms}
\end{equation}
with $u=\log(1+T/T_{\mathrm{ref}})$ and $v=\log(D_U/D_{\mathrm{ref}})$ for reference constants fixed on
the development set: one parameter for mathematics and code, which vanish at zero budget, and three for question
answering. Pre-specified structural checks neither reject nor establish an additive joint form in
budget and pool (Appendix~\ref{sec:app_selbound}), so the forms rest on the development comparison:
leave-one-run-out selected the logarithm of the reuse ratio for mathematics and code and the joint
budget and pool form only for question answering (Appendix Table~\ref{tab:distill_confirm}), a local
approximation at the tested budgets and pools. The validation target is new pools and budgets of the
development students (Section~\ref{sec:unseen_settings}). The relations describe the Gemma-3 270M and
1B students under one recipe, with pools to 158k tokens, budgets of 50k--200k and question answering
scored on 2Wiki. There question answering improves under both training recipes tested while mathematics
and code move by hundredths to tenths of a nat; at high reuse and on the other question-answering distributions the response
reverses (Section~\ref{sec:why}), and the 4B student and reuse near fourteen lie outside this domain.
